# v0.7.12 后续工程加固验证

日期：2026-08-23  
平台：Windows x64，Visual Studio 18 / MSVC 14.51，Release

## 改动边界

- TSF 敏感输入域透传：密码、私密、数字密码和 PIN；
- 313 条结构化语料真实执行及 13 条已知缺口基线；
- 语义版本与提交级精确构建 ID 分离；
- Shim、Host、词库三层 SHA-256 诊断；
- 当前状态和下一阶段文档校正。

## 自动验证

```powershell
cmake -S . -B build/windows-x64
cmake --build build/windows-x64 --config Release --parallel 1
ctest --test-dir build/windows-x64 -C Release --output-on-failure
```

结果：Release 全目标编译通过；CTest **63/63 通过**。

结构化语料运行器的结果：

```text
executed=313
rank=46
known_missing=13
results=84
smoke=170
entries=928712
```

精确构建身份验证：

```powershell
PiInputHost.exe --version
PiInputHost.exe --build-id
PiInputHost.exe --health <build-id>
```

`--version` 输出 `0.7.12`；`--build-id` 输出 `0.7.12+<12位提交号>`，工作区有未提交改动时追加 `-dirty`；匹配的健康检查成功，错误 ID 返回失败。

## 尚需人工验收

敏感输入策略已有单元测试和源码边界回归，但自动测试不能代替真实应用对 `InputScope` 的标注行为。扩大用户测试前仍需在 Chromium/Edge、Firefox、Win32、WinUI/UWP 和系统凭据界面逐项确认：按键透传、无候选、无组合、无 Host 内容请求、无学习记录。
