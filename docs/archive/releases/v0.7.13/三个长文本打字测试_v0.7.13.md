# 三个长文本打字测试（v0.7.13）

v0.7.13 延续三组真实文本逐键回放门禁，并把 13 个专业术语纳入结构化候选回归。构建发布候选时，`piinput-typing-latency-tests` 必须继续满足逐键 P95 与最大延迟上限；候选覆盖由 313 个结构化用例单独判定。

本版本的最终 CTest 数量、逐键延迟和完整 8 小时资源曲线在候选包构建完成后记录到 `VERIFICATION_v0.7.13.md` 与 `artifacts/soak/`，不以旧版本记录代替。
