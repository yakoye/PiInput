# PiInput v0.5.8-dev 验证记录

本文件记录 v0.5.8-dev 的实际构建与测试证据。最终发布前必须填写完整，不能用计划值代替实测结果。

## 已完成的定向验证

- 修复前 80 次连续请求：P95 约 15.7 ms，最大约 20.1 ms；
- 修复后 64 位路径 160 次请求：P95 1.327 ms，最大 2.048 ms；
- 修复后 32 位路径 160 次请求：P95 0.726 ms，最大 0.986 ms；
- `piinput-host-process` 连续执行 5 次通过；
- 真实词库覆盖“我们明天、去哪里、这个是、你觉得、怎样”。

## Release 验证

- Visual Studio 18 / MSVC Release 全目标构建成功；
- 全量 CTest：57/57 通过，0 失败，总耗时 52.71 秒；
- 外部真实二进制词库增量、长全拼、词库回归和 3500/7000 汉字覆盖均通过；
- `piinput-host-process` 新增连续传输和五个真实短语门禁并通过；
- CMake 安装布局成功生成 Host、稳定 TSF DLL、安装器、卸载器、设置程序和运行数据；
- `PiInputHost.exe --build-id` 输出 `0.5.8`；
- `piinput-base.lex` 为 4,004,276 字节，继续由常驻 Host 统一加载；
- 文件清单与 SHA-256 门禁在最终文档更新后重新生成并复验。

## 中文符号边界实机复测（2026-08-16）

- 系统实际 Host：`0.5.8`，注册表 `CurrentHostPath`、运行中 Host 健康检查和构建号一致；
- Notepad++ 真实物理按键输入：`我，他们。`、`我们明天，去哪里？`、`（我们），明天。`、`你：我；他！`；
- Notepad4 真实物理按键输入：`我，他们。`、`我们明天，去哪儿？`、`（我们），你觉得？怎样！`；
- Notepad++ 连续中文/符号混输 62 键：自动化观察 P95 162.47 ms，最大 195.13 ms，0 次超过 500 ms；该值包含窗口自动化同步开销；
- Notepad4 连续中文/符号混输 52 键：自动化观察 P95 88.05 ms，最大 201.40 ms，0 次超过 500 ms；
- Host 进程级 `我，他们。` 原子链路共 10 次请求：P95 1.283 ms，最大 1.708 ms；
- 同一链路连续执行 20 轮全部成功；各轮 P95 的最大值 2.242 ms，所有单次请求的最大值 4.131 ms；
- `punctuation_cases.tsv` 覆盖主键盘全部数字行与标点键的中文、英文和程序员模式；`piinput-core-tests` 实测通过；
- 新增 `--punctuation-chain` 回归门禁，验证“中文候选 + 中文逗号 + 中文候选 + 中文句号”提交、确认及下一组合区建立均低于 P95 10 ms、最大 20 ms；
- 候选准确率观察：`qunali` 在一次实机输入中首选为“去哪儿”，属于排序/准确率问题，不属于本次符号边界性能问题。
