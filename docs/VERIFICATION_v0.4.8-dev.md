# PiInput v0.4.8-dev 验证记录

日期：2026-08-14  
平台：Windows 11 x64、MSVC 19.51、Windows SDK 10.0.26100.0

## 自动验证

- Release 全目标构建：通过；
- CTest（暂不含生成型 SHA 门禁）：50/50 通过，0 失败；
- 中文、英文、程序员符号表：通过；
- 搜狗默认与微信可选括号风格：通过；
- `;;f` 与双反引号加 `f` 符号中心：通过；
- 旧 `;sheshidu` 兼容：通过；
- 单反引号 `·`、三反引号 Markdown、未知命令不吞键：通过；
- 默认一行、逐行展开和候选网格方向键：通过；
- 600 DIP 窗口上限、目标 DPI 缩放和长候选列数调整：通过；
- 稳定 TSF shim 反引号转发源码门禁：通过；
- 外部真实词库增量性能、SCEL、段落输入和 7000 字覆盖门禁：通过。

完整 50 项测试耗时：157.47 秒。

## 真实应用验证

发布包安装后验证以下应用：

| 应用 | 中文符号 | 符号命令 | 一行/展开 | 文本光标位置 | 结果 |
|---|---|---|---|---|---|
| Notepad4 | 待执行 | 待执行 | 待执行 | 待执行 | 待执行 |
| Notepad++ | 待执行 | 待执行 | 待执行 | 待执行 | 待执行 |
| Microsoft Word | 待执行 | 待执行 | 待执行 | 待执行 | 待执行 |

## 安装与打包

安装包、ZIP 哈希和最终 SHA 源码门禁在真实应用验证后补充。

