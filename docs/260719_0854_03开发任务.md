# PiInput 开发任务总表

更新时间：2026-08-23。状态说明：`[x]` 已实现并进入自动回归；`[~]` 已实现主体，仍需真机或长期验证；`[ ]` 尚未完成。

## 当前可用基线（v0.7.12）

- [x] Windows x64 原生 TSF 输入法，稳定 Shim 与独立 Host 进程；
- [x] 全拼、小鹤、自然码、微软双拼、智能 ABC；
- [x] 928,712 条离线词库、增量候选、句子解码、用户学习；
- [x] 中文/英文/程序员标点、符号、日期时间和农历候选；
- [x] 原生候选窗、任务栏语言栏、状态提示、托盘菜单；
- [x] 五页 31 项原生设置界面与配置热加载；
- [x] 原生安装、升级回滚、卸载、旧数据迁移和 AppContainer 访问授权；
- [x] Host 重启恢复、会话上限、IPC 边界与按键延迟门禁；
- [x] 63 项 CTest，含外部大词库和真实段落性能回归。

## 本轮工程加固

- [x] 查询 `GUID_PROP_INPUTSCOPE`，对密码、私密、数字密码和 PIN 输入域完全透传；
- [x] 敏感输入域不联系 Host、不显示候选、不创建组合、不触发学习；
- [~] 敏感输入已覆盖策略单测和源码边界回归，仍需在浏览器、Win32、WinUI/UWP 的真实密码框逐项验收；
- [x] 313 条结构化语料进入真实引擎执行，不再只校验 JSON 数量；
- [x] 46 条专业词排名作为硬门槛，13 条未达目标作为显式已知缺口基线；
- [x] Host 健康响应分离语义版本与精确构建 ID（提交号，脏工作区带 `-dirty`）；
- [x] 安装器按精确构建 ID 验证新 Host，避免同版本错连旧进程；
- [x] 诊断 JSON 同时给出 Shim、Host、词库路径和 SHA-256。

## P0：扩大用户测试前

- [ ] 完成真实密码框矩阵：Chromium/Edge、Firefox、原生 Win32、WinUI/UWP、Windows 登录与凭据界面；
- [ ] 明确项目源码和随包词库的许可证、来源、版本及再分发义务；
- [ ] 建立 Windows CI：Release 构建、63 项测试、干净包内容和 SHA-256 校验；
- [ ] 为安装器、Host、TSF DLL 和卸载器提供正式代码签名；
- [ ] 做 8 小时以上输入/切换/休眠恢复稳定性测试，并记录崩溃率、句柄和内存曲线；
- [ ] 对最终用户包执行安装、升级、回滚、卸载和公开下载哈希闭环验证。

## P1：输入质量和资源占用

- [ ] 补齐 `known_missing_professional_targets.json` 中 13 个专业词，并删除对应基线项；
- [ ] 建立真实首选错误黄金集，持续统计 Top-1、Top-5 和平均排名；
- [ ] 将当前仅做无崩溃验证的纠错、模糊音用例升级为结果断言；
- [ ] 增加二元搭配或轻量上下文模型，同时保留普通高频词优先约束；
- [ ] 将大词库加载改为只读内存映射或更紧凑索引，降低约 38 MB 词库的冷启动和私有内存；
- [ ] 完成用户词置顶、屏蔽、误学删除、导入导出与长期衰减；
- [ ] 增加日志脱敏和可选择的敏感应用黑名单作为第二层保护。

## P2：兼容性和后续平台

- [ ] Windows x86 宿主兼容；
- [ ] Windows ARM64 原生构建；
- [ ] 深色模式、触屏和辅助功能专项验证；
- [ ] 稳定 C ABI；
- [ ] macOS、Android、iOS 输入法实现；
- [ ] 跨设备端到端加密同步。

跨平台和同步仍排在 Windows 准确率、隐私、稳定性与发布合规之后。
