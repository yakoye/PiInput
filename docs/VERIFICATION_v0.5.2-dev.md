# PiInput v0.5.2-dev 验证记录

## 已完成的定向验证

- 增量解码 Release 测试通过；
- 真实 101,469 词二进制词库：`mkt` 首选“明天”；
- `rug` 前三项为“如果、入股、乳沟”；
- `xnhe` 首选“小河”；
- `yuwh` 首选“欲望”；
- `hlheruhdlq` 首选“黄河入海流”；
- `biruwoycuibuv` 第一页包含“比如我要是不知道”；
- 完整 `biruwoycuibuvidc` 首选“比如我要是不知道”。
- 候选框八向工作区边界测试通过：上部五个方向显示在光标下方，底部三个方向在空间不足时翻转到光标上方；
- 100%、125%、200% DPI 的光标间距和工作区夹紧测试通过；
- 三组真实文本已抽取九个代表项，与搜狗小鹤在 Notepad++ 中进行物理按键对照，详见 `docs/三组文本与八向边界对照测试_2026-08-15.md`。

## 性能样本

词库：101,469 词、4,004,169 字节。

| 输入 | P50 | P95 | P99 |
|---|---:|---:|---:|
| `mkt` | 46.1 µs | 101.2 µs | 164.5 µs |
| `rug` | 45.0 µs | 100.8 µs | 187.9 µs |
| `yuwh` | 13.4 µs | 24.4 µs | 36.9 µs |
| `hlheruhdlq` | 43.9 µs | 81.1 µs | 112.6 µs |
| `biruwoycuibuv` | 206.5 µs | 507.8 µs | 775.8 µs |

以上为核心热查询，不等同于完整 Windows 按键到绘制延迟。

## 发布前最终门禁

- Windows Release 全目标编译成功，包括稳定 TSF Shim、独立 Host、设置、安装器、卸载器和全部工具；
- 真实语料 205 个片段：全拼 Top 10 命中 102（49.76%），小鹤 Top 10 命中 103（50.24%），共同命中 102（49.76%）；
- 首轮完整门禁的 52 个功能测试全部通过；发布清单最初因 ASCII 无法保存中文路径而失败，现已改为 UTF-8 并重新生成；
- 最终 53 项 CTest、安装布局和 SHA-256 结果以本轮文件末尾记录为准。

## 最终结果

候选框八向边界修复完成后，Windows Release 全目标重新构建成功；完整 CTest 为 `53/53` 通过、`0` 失败，总耗时 312.01 秒。候选框八向边界、真实语料、外部增量性能、词库、安装/卸载、稳定 Host 和发布元数据门禁均包含在本轮结果中。

修复前生成的 v0.5.2 ZIP 已作废，最终发布包使用重新安装布局和重新压缩后的 SHA-256。
