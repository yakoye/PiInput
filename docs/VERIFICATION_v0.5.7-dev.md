# PiInput v0.5.7-dev 验证记录

验证日期：2026-08-16  
平台：Windows x64、Visual Studio 18 2026、MSVC、Windows SDK 10.0.26100.0

## 定向验证

- 候选整词优先、词语耗尽后追加分段取字：通过；
- 进入取字层后保留完整词候选与稳定顺序：通过；
- `=` 逐行移动、选中每行第一项、连续滚动使旧行离开视野：通过；
- 展开行数 3、4、5、6：通过；
- 当前行显示数字、非当前行不显示数字：通过；
- 设置程序可选择 1～6 个展开行并持久保存：通过；
- Windows TSF 注册名称精确为“PiInput 中文输入法”，不含“开发版”后缀：通过；
- 一次成功分段组合后立即学习并进入下一次第一行：通过；
- 用户模型保存、重新加载和提交确认：通过；
- 全拼/小鹤“开悟”“丝滑”基础词库：通过；
- 真实 101,472 条二进制词库与 3500/7000 汉字覆盖：通过。

## 性能证据

真实词库：`dicts/cache/piinput-base.lex`，101,472 条。

长全拼：`wojintianxiawuyaoquchaoshimaidianshuiguo`

- 修复前 P95：约 15.9 ms；
- 修复后 P50：约 1.8 ms；
- 修复后 P95：约 2.5 ms；
- 修复后 P99：约 2.7 ms；
- 修复后最大值：约 4.8 ms。

短查询 10,000 次门禁：

- P50：约 27.7 us；
- P95：约 33.4 us；
- P99：约 71.6 us。

## 真实词库查询

- 全拼 `kaiwu`：首选“开悟”；
- 小鹤 `kdwu`：首选“开悟”；
- 全拼 `sihua`：首选“丝滑”；
- 小鹤 `sihx`：首选“丝滑”；
- 全拼 `mingtian` 与小鹤 `mktm`：首选“明天”。

## 最终验证

- Windows Release 从干净构建目录重新配置并编译全部目标：通过；
- 完整 CTest：`57/57 passed`，`0 failed`，总耗时 `51.38 s`；
- 外部大词库增量、长全拼性能、段落输入、7000 字覆盖、Host 进程与重启交接门禁：通过；
- Host 性能门禁分离 Windows 测试进程调度与真实按键往返；每次按键到 Host 回复的最慢值必须小于 100 ms：通过；
- 发布元数据、Windows 源码结构、安装器、卸载器、设置与词库脚本门禁：通过；
- Windows 发布 ZIP 已生成并重新解压核验，安装器、卸载器、测试台、Host、TSF DLL、基础词库和中文使用指南齐全；
- 从重新解压的发布包直接查询内置二进制词库：全拼 `kaiwu` 首选“开悟”，小鹤 `sihx` 首选“丝滑”；
- 按用户当前要求，本轮不自动安装、不推送仓库，系统实机输入由用户运行安装器后继续反馈。
