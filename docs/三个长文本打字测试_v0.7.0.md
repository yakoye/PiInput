# 三个长文本打字测试（v0.7.0）

v0.7.0 沿用真实命名管道逐键回放门禁，覆盖 `tests/data/real_world_text_corpus.txt` 中三个长文本、翻行、数字选字、空格提交与标点提交。

本版特别关注提交边界：成功送达但无回复的 `commit_result` 不得触发 750 毫秒重试，后续按键不得被单工作队列阻塞。完整 Release CTest 的 `piinput-typing-latency` 结果记录在 `VERIFICATION_v0.7.0.md`。

该自动回放覆盖真实 Host、真实协议和真实引擎；目标应用自己的 TSF 行为由用户在 Notepad++ 等真实应用中补充验证。
