# PiInput v0.4.9-dev 验证记录

日期：2026-08-15  
平台：Windows 11 x64、MSVC 19.51、Windows SDK 10.0.26100.0

## 自动验证

- Release 全目标构建：通过；
- 完整 CTest（含 SHA 与发布元数据门禁）：51/51 通过，0 失败；
- 中文、英文、程序员符号表：通过；
- 搜狗默认与微信可选括号风格：通过；
- `;;f` 与双反引号加 `f` 符号中心：通过；
- 旧 `;sheshidu` 兼容：通过；
- 单反引号、Markdown 行内代码、三反引号代码块和未知命令不吞键：通过；
- 默认一行、逐行展开和候选网格方向键：通过；
- 600 DIP 窗口上限、目标 DPI 缩放和长候选列数调整：通过；
- 稳定 TSF shim 反引号转发源码门禁：通过；
- 外部真实词库增量性能、SCEL、段落输入和 7000 字覆盖门禁：通过。

最终完整 51 项测试耗时：132.10 秒。

### Notepad++ 定位问题的红—绿证据

- 真实复现：旧入口能显示一行候选，但应用选择区没有提供可用文本矩形，候选退回到鼠标附近；
- RED：新增源码回归，要求稳定入口优先查询自己拥有的 Composition 范围；旧实现按预期失败；
- GREEN：`capture_composition_caret` 先用 `ITfComposition::GetRange`，失败时才退回应用 selection；`PiInputTSF.dll` Release 构建及 Windows 源码门禁通过。

## 真实应用验证

发布包安装后验证以下应用：

| 应用 | 中文符号 | 符号命令 | 一行/展开 | 文本光标位置 | 结果 |
|---|---|---|---|---|---|
| Notepad4 | 待 0.4.9 安装后执行 | 待执行 | 待执行 | 待执行 | UAC 未确认，未伪报通过 |
| Notepad++ | 待 0.4.9 安装后执行 | 待执行 | 一行通过 | 旧入口复现降级位置；源码已修 | 待新版独立进程复测 |
| Microsoft Word | 待 0.4.9 安装后执行 | 待执行 | 一行通过 | 标准 TSF 光标定位通过 | 基础候选与位置通过 |

## 安装与打包

发布包：`artifacts/PiInput-v0.4.9-dev-windows-x64.zip`，同目录 `.sha256.txt` 保存最终校验值。

0.4.9 安装需要 Windows UAC 人工确认。当前没有绕过或关闭 UAC；因此本记录明确区分“自动通过”“旧入口实测”和“新版安装后待复测”。
