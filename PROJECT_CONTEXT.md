# LiteIME 项目完整上下文

本文档是跨会话、跨开发环境继续迭代的权威入口。每个发布压缩包必须保留并更新。

## 1. 用户目标

用户需要一个中文输入法：

- 安装包体积小；
- 词库多，但不能让冷门专业词污染普通候选；
- 输入准确、效率高、性能高；
- 简单、干净、无广告；
- 无 AI、无语音；
- Windows 首发；
- 支持全拼和多种双拼，小鹤优先但不是唯一方案；
- 可以解析用户自行导入的搜狗 `.scel`；
- 需要比 Windows 自带面板更快的自有符号系统；
- 后期支持跨设备用户词库、设置、短语和文本剪贴板同步；
- 将来考虑 macOS、Android、iOS。

## 2. 不变的产品底线

1. 候选准确率优先于词库绝对数量；
2. 相同输入、相同快照下候选不能异步跳变；
3. 按键热路径禁止网络访问；
4. 基础输入离线完整可用；
5. 专业词库与通用词库必须分层；
6. 用户能够删除错误学习、屏蔽和置顶候选；
7. 支持从拼音中间修改；
8. 中文输入中能够快速输入 ASCII 标点；
9. 符号既能分类浏览，也能按中文、拼音和英文搜索；
10. 同步必须可关闭，最终端到端加密；
11. 不保存完整按键记录和完整输入历史；
12. 不加入 AI、语音、广告、资讯和内容推荐。

## 2.1 v0.2.0 当前交互基线

- 候选横向显示；
- 单音节默认每页 9 项，多音节默认每页 6 项；
- `-` 上一页，`=` 下一页，同时保留 PageUp/PageDown；
- 单独按 Shift 切换中英文，Shift 参与组合快捷键时不切换；
- 页大小写入 `%LOCALAPPDATA%\LiteIME\UserData\settings.ini`，范围 1～9；
- 会话保留 90 个不可变候选，支持稳定多页浏览；
- phrase-pinyin-data 只提供读音补全，不能用统一默认权重覆盖 Rime 等真实词频。

已确认的回归词包括：`接触`、`词汇`、`感觉`、`现在`、`中国`。完整真实长句语料与未来诊断项分别保存在 `tests/data/real_world_text_corpus.txt` 和 `tests/data/diagnostic_input_cases.tsv`。

## 3. 技术栈决策

- 核心：C++20；
- 构建：CMake；
- Windows：C++20 + Win32 + COM + TSF；
- Windows 候选窗口：优先 DirectWrite/Direct2D；
- Windows 设置程序：首版原生 C++；
- 开发自动化：PowerShell；
- Android：Kotlin + JNI；
- macOS/iOS：Swift + Objective-C++；
- 跨语言边界：稳定 C ABI；
- Python 只允许作为开发期数据分析工具，不能成为最终运行依赖。

核心层不得包含 Windows 专用逻辑。平台代码必须位于 `platform/<platform>`。

## 4. 总体架构

```text
平台接入层
Windows TSF / macOS InputMethodKit / Android IME / iOS Keyboard
                         │
                         ▼
                  稳定 C ABI 边界
                         │
                         ▼
                  C++20 ime_core
全拼 / 多双拼 / 拼音切分 / 词库检索 / 句子解码 / 排序
用户学习 / 标点 / 符号 / 候选快照 / 同步数据模型
                         │
                         ▼
基础词库 / 专业词库 / 用户词库 / 设置 / 同步变更日志
```

## 5. 双拼决策

- 词库只保存标准拼音，例如 `ji'suan'ji`；
- 全拼和双拼都是“输入编码 → 标准拼音”的前端；
- 当前内置：小鹤、自然码、微软、智能 ABC；
- 小鹤是第一优先测试方案，但架构不能绑定小鹤；
- 后续增加搜狗、紫光及用户自定义方案；
- 新增方案不得修改词库和句子解码器；
- 自定义方案需要导入、导出、冲突检测和恢复默认。

## 6. 词库决策

- SCEL 只作为导入格式，运行时不直接解析 SCEL；
- 当前流程：SCEL → TSV → `.lex`；
- `.lex` 当前为自有小型二进制格式；
- 编译器按 `word+pinyin` 去重，排序后保留最高权重；
- 批量导入会保留每个独立 `.lex`，同时生成 `liteime-imported.lex` 合并词库；
- 正式安装包不默认重新分发授权不明确的第三方完整词库；
- 后续需要词库来源、许可证、质量报告、分层权重和内存映射索引。

用户提供的真实回归词库：

- `电子词汇大全【官方推荐】.scel`：5,596 条，413 个拼音项；
- `计算机词汇大全【官方推荐】.scel`：10,300 条，413 个拼音项。

## 7. 标点与符号决策

符号是 Windows 首版范围，不是很晚的附加功能。

三层入口：

1. 中文、英文、程序员标点直接转换；
2. 在普通候选栏按中文名、拼音、英文名和别名搜索；
3. 完整符号面板，支持最近、收藏、自定义和键盘操作。

当前核心已经实现三种标点转换和 140 个符号搜索；完整符号面板、收藏和最近使用尚未接入系统输入法界面。

## 8. 候选稳定性决策

- 候选列表由 `generation` 标识；
- 每个候选 ID 包含快照 generation；
- 输入未改变时不能生成新快照；
- 旧快照候选 ID 必须拒绝；
- 后台词库、学习写盘和同步不能改变已经显示的列表；
- 用户主动按键或选词后才允许生成下一版候选。

当前 `ImeSession` 已实现上述基础约束。

## 9. 用户数据和同步决策

同步最后开发，但数据模型必须预留：

- object UUID；
- device UUID；
- 创建/修改时间；
- 删除标记；
- schema version；
- change event ID；
- 冲突版本。

同步顺序：

1. 用户词库和设置；
2. 常用短语；
3. 收藏符号；
4. 文本剪贴板；
5. 剪贴板历史和高级规则。

最终同步端到端加密，中继只保存密文。

## 10. 本地目录约定

版本压缩包：

```text
lite-ime-vX.Y.Z-dev.zip
└── lite-ime-dev
```

用户本地始终使用固定目录：

```text
C:\...\lite-ime\lite-ime-dev
```

用户数据：

```text
%LOCALAPPDATA%\LiteIME\UserData
├── lexicons
├── user_model.tsv
├── settings
├── symbols
└── sync
```

开发程序：

```text
%LOCALAPPDATA%\LiteIME\Dev
```

删除源码目录或升级开发包不能删除用户数据。

## 11. v0.2.0-dev 当前实现状态

v0.2.0 冻结新功能，只完善输入基础。新增完整小鹤零声母和 413 音节覆盖测试、全拼同词表测试、开源词库构建器、外部 `dicts` 下载缓存、确定性对数词频排序和性能门槛。Windows 真机已从四个开源来源及两个本地 SCEL 构建 459,505 条 `.lex`，`gjjt → 感觉`、`xmzd → 现在` 均为首选，`wo` 的 10,000 次核心查询 P95 远低于 2 ms。

外部目录固定为源码同级 `dicts/`，其中 `sources/` 保存自动下载仓库，`scel/` 和根目录兼容用户 SCEL，`user/` 保存自定义 TSV，`generated/` 保存统一 TSV，`cache/` 保存已验证二进制词库。`THUOCL` 会下载到本地，但默认不混入通用候选。

### v0.1.6-dev 历史实现状态

### 已实现并由跨平台自动测试验证

- C++20/CMake 工程和完整文档体系；
- SCEL 元数据、拼音表、词条、权重解析；
- SCEL → TSV/JSONL/TXT；
- 标准普通话音节表；
- 全拼连续切分和 `'` 手动边界；
- 小鹤、自然码、微软、智能 ABC 双拼；
- 自有 `.lex` 格式和编译工具；
- 多 SCEL 批量导入和合并；
- 多词句子 Beam 解码；
- 基础词频和本地用户选择加权；
- 本地用户模型原子写入；
- 候选快照、候选唯一 ID 和过期候选拒绝；
- 中间插入、删除和基本光标移动；
- 中文、英文、程序员标点核心；
- 140 个符号及中英文/拼音搜索；
- CLI、词库工具、性能基准；
- Visual Studio 2026/2022 自动构建脚本；
- 根目录一键构建、测试、导入和开发安装；
- 内置起步基础词库；
- 基础词库与专业 SCEL 强制合并；
- `jisuanji` 和小鹤 `jisrji` 返回“计算机”的回归测试；
- Linux Release、真实 SCEL 回归和 ASan/UBSan 验证。

### 已有 Windows 源码实现，但仍需用户 Windows/MSVC 真机验证

- `LiteImeTSF.dll` COM/TSF 文本服务；
- 简体中文语言配置文件注册、启用和激活；
- `ITfKeyEventSink`；
- TSF Composition 创建、更新、提交和取消；
- GDI 最小候选窗口；
- Space、数字键、方向键和翻页；
- Backspace/Delete/Home/End/左右移动；
- Enter 原始拼音提交、Esc 取消；
- TSF 内符号搜索；
- 本地选词学习；
- `liteime-profile.exe` 方案设置和配置文件激活；
- 注册、修复、验证和卸载脚本；
- 改进后的独立 Win32 预览。

上述 Windows TSF 部分不能在 Linux 开发环境中声称已经编译或兼容所有应用。用户在 Visual Studio 2026 环境中的第一轮真实构建已经确认跨平台核心、测试、词库工具、CLI、benchmark 和 preview 可以编译。第一轮 TSF 阻断为错误创建 Profile Manager 和错误链接 `msctf.lib`，v0.1.5 已修复。随后用户运行修复注册脚本时发现，旧 profile 不存在或未激活会返回 `0x80004005`，而 PowerShell 将其当成致命错误，导致重新注册根本没有继续执行。v0.1.6 已将清理流程改为幂等 best-effort，并切换到 `ITfInputProcessorProfileMgr::RegisterProfile`，同时增加注册状态检查。等待下一轮 Windows 构建和注册验证。

### 尚未完成，不能声称已完成

- Windows TSF 在用户机器的第二轮编译、注册和记事本输入验证；
- Visual Studio、VS Code、Chrome、Office、微信、Terminal 等应用兼容；
- 32 位宿主应用和 Windows ARM64；
- DirectWrite/Direct2D 候选窗；
- 精确 TSF 插入点定位、高 DPI、多显示器和深色模式；
- 中英文模式切换和状态栏；
- 标点转换接入 TSF；
- 已选词与未选拼音混合编辑；
- 按音节移动、非法拼音高亮、Tab 切分；
- 高质量通用基础词库、单字词库和二元语言模型；
- 词库分层权重、置顶、屏蔽和错误学习管理 UI；
- 完整符号面板、收藏、最近和自定义；
- 设置程序；
- 独立引擎进程与 IPC；
- 正式 Windows 安装器和代码签名；
- macOS、Android、iOS；
- 跨设备同步。

## 12. 下一阶段

`v0.2.0-dev` 已打通系统输入和通用词库基础。下一阶段仍不扩展功能，优先：

```text
收集真实首选错误样例
→ 建立候选排序黄金集
→ 归一化不同词库来源权重
→ 测量 TSF 按键到候选端到端延迟
→ 优化冷启动和大词库内存占用
→ 扩充全拼与小鹤回归样例
```

全拼、小鹤、准确率和延迟达到可日用标准以前，不开发跨设备同步和移动端。

## 13. 每版发布包硬性要求

每个发布包必须包含：

- 完整源码；
- `PROJECT_CONTEXT.md`；
- 架构、任务、约束；
- 版本说明；
- 带目标版本号的下一版计划；
- 新会话续接说明；
- 构建、测试和安装说明；
- 实际验证记录；
- Git 命令；
- `VERSION`、文件清单和 SHA-256 清单。

不得只交付差异文件。源码开发包不得携带旧 `build/`、`dist/`、`.vs/`、二进制和 CMake 缓存。
