# PiInput 项目完整上下文

本文档是跨会话、跨开发环境继续迭代的权威入口。每个发布压缩包必须保留并更新。

## 1. 用户目标

用户需要一个中文输入法：

- 安装包体积小；
- 词库多，但不能让冷门专业词污染普通候选；
- 输入准确、效率高、性能高；
- 简单、干净、无广告；
- 无 AI、无语音；
- Windows 首发；
- 支持全拼和多种双拼，小鹤优先但不是唯一方案；
- 可以解析用户自行导入的搜狗 `.scel`；
- 需要比 Windows 自带面板更快的自有符号系统；
- 后期支持跨设备用户词库、设置、短语和文本剪贴板同步；
- 将来考虑 macOS、Android、iOS。

## 2. 不变的产品底线

1. 候选准确率优先于词库绝对数量；
2. 相同输入、相同快照下候选不能异步跳变；
3. 按键热路径禁止网络访问；
4. 基础输入离线完整可用；
5. 专业词库与通用词库必须分层；
6. 用户能够删除错误学习、屏蔽和置顶候选；
7. 支持从拼音中间修改；
8. 中文输入中能够快速输入 ASCII 标点；
9. 符号既能分类浏览，也能按中文、拼音和英文搜索；
10. 同步必须可关闭，最终端到端加密；
11. 不保存完整按键记录和完整输入历史；
12. 不加入 AI、语音、广告、资讯和内容推荐。

## 2.1 v0.2.0 当前交互基线

- 候选横向显示；
- 单音节默认每页 9 项，多音节默认每页 6 项；
- `-` 上一页，`=` 下一页，同时保留 PageUp/PageDown；
- 单独按 Shift 切换中英文，Shift 参与组合快捷键时不切换；
- 页大小写入 `%LOCALAPPDATA%\PiInput\UserData\settings.ini`，范围 1～9；
- 会话保留 90 个不可变候选，支持稳定多页浏览；
- phrase-pinyin-data 只提供读音补全，不能用统一默认权重覆盖 Rime 等真实词频。

已确认的回归词包括：`接触`、`词汇`、`感觉`、`现在`、`中国`。完整真实长句语料与未来诊断项分别保存在 `tests/data/real_world_text_corpus.txt` 和 `tests/data/diagnostic_input_cases.tsv`。

不完整输入也必须产生稳定候选。当前规则是精确解析优先；精确解析为空时，对最后一个未完成音节做有界前缀检索。已确认小鹤 `mkt→明天`、`rug→如果/入股`，全拼尾音节前缀使用同一机制。

中文模式的完整 ASCII 标点已接入 TSF。组合状态的 `-`/`=` 用于候选行导航；可靠普通候选耗尽后，`=` 进入分段取字。单引号仍可作拼音边界；其他标点会先提交当前候选再提交中文标点。英文和程序员模式保持 ASCII。

## 3. 技术栈决策

- 核心：C++20；
- 构建：CMake；
- Windows：C++20 + Win32 + COM + TSF；
- Windows 候选窗口：优先 DirectWrite/Direct2D；
- Windows 设置程序：首版原生 C++；
- 开发自动化：PowerShell；
- Android：Kotlin + JNI；
- macOS/iOS：Swift + Objective-C++；
- 跨语言边界：稳定 C ABI；
- Python 只允许作为开发期数据分析工具，不能成为最终运行依赖。

核心层不得包含 Windows 专用逻辑。平台代码必须位于 `platform/<platform>`。

## 4. 总体架构

```text
平台接入层
Windows TSF / macOS InputMethodKit / Android IME / iOS Keyboard
                         │
                         ▼
                  稳定 C ABI 边界
                         │
                         ▼
                  C++20 ime_core
全拼 / 多双拼 / 拼音切分 / 词库检索 / 句子解码 / 排序
用户学习 / 标点 / 符号 / 候选快照 / 同步数据模型
                         │
                         ▼
基础词库 / 专业词库 / 用户词库 / 设置 / 同步变更日志
```

## 5. 双拼决策

- 词库只保存标准拼音，例如 `ji'suan'ji`；
- 全拼和双拼都是“输入编码 → 标准拼音”的前端；
- 当前内置：小鹤、自然码、微软、智能 ABC；
- 小鹤是第一优先测试方案，但架构不能绑定小鹤；
- 后续增加搜狗、紫光及用户自定义方案；
- 新增方案不得修改词库和句子解码器；
- 自定义方案需要导入、导出、冲突检测和恢复默认。

## 6. 词库决策

- SCEL 只作为导入格式，运行时不直接解析 SCEL；
- 当前流程：SCEL → TSV → `.lex`；
- `.lex` 当前为自有小型二进制格式；
- 编译器按 `word+pinyin` 去重，排序后保留最高权重；
- 批量导入会保留每个独立 `.lex`，同时生成 `piinput-imported.lex` 合并词库；
- 正式安装包不默认重新分发授权不明确的第三方完整词库；
- 后续需要词库来源、许可证、质量报告、分层权重和内存映射索引。

用户提供的真实回归词库：

- `电子词汇大全【官方推荐】.scel`：5,596 条，413 个拼音项；
- `计算机词汇大全【官方推荐】.scel`：10,300 条，413 个拼音项。

## 7. 标点与符号决策

符号是 Windows 首版范围，不是很晚的附加功能。

三层入口：

1. 中文、英文、程序员标点直接转换；
2. 在普通候选栏按中文名、拼音、英文名和别名搜索；
3. 完整符号面板，支持最近、收藏、自定义和键盘操作。

当前核心已经实现三种标点转换和 140 个符号搜索；完整符号面板、收藏和最近使用尚未接入系统输入法界面。

## 8. 候选稳定性决策

- 候选列表由 `generation` 标识；
- 每个候选 ID 包含快照 generation；
- 输入未改变时不能生成新快照；
- 旧快照候选 ID 必须拒绝；
- 后台词库、学习写盘和同步不能改变已经显示的列表；
- 用户主动按键或选词后才允许生成下一版候选。

当前 `ImeSession` 已实现上述基础约束。

## 9. 用户数据和同步决策

同步最后开发，但数据模型必须预留：

- object UUID；
- device UUID；
- 创建/修改时间；
- 删除标记；
- schema version；
- change event ID；
- 冲突版本。

同步顺序：

1. 用户词库和设置；
2. 常用短语；
3. 收藏符号；
4. 文本剪贴板；
5. 剪贴板历史和高级规则。

最终同步端到端加密，中继只保存密文。

## 10. 本地目录约定

版本压缩包：

```text
piinput-vX.Y.Z-dev.zip
└── piinput-dev
```

用户本地始终使用固定目录：

```text
C:\...\piinput\piinput-dev
```

用户数据：

```text
%LOCALAPPDATA%\PiInput\UserData
├── lexicons
├── user_model.tsv
├── settings
├── symbols
└── sync
```

开发程序：

```text
%LOCALAPPDATA%\PiInput\Dev
├── current.txt
└── versions
    └── <版本-构建号>
        ├── bin
        └── data
```

开发安装必须使用 `PiInput-Install.exe` 的版本并存方案。禁止重新覆盖 `%LOCALAPPDATA%\PiInput\Dev\bin\PiInputTSF.dll`，禁止为了更新 DLL 强制关闭用户应用。用户数据始终位于 `%LOCALAPPDATA%\PiInput\UserData`，不能放进版本目录。

## 10.1 v0.2.0 测试语料

用户提供的 `piinput-test-corpus-v0.2.0.zip` 已完整纳入 `tests/corpus/v0.2.0`。语料包含 407 个标准全拼音节和 786 条结构化用例。当前能力作为阻断门禁；纠错、模糊音、V/U 模式和更完整的语言模型仅作为未来诊断，不得据此宣称已经支持。

删除源码目录或升级开发包不能删除用户数据。

## 11. v0.3.9-dev 当前实现状态

`v0.3.9-dev` 在现有安全安装、延迟加载和全拼/小鹤基础上增加：选择性合并 THUOCL 诗词与成语；完整长词精确命中优先；候选来源和路径证据；普通候选耗尽后的组合区分段取字；按中文词条和标准拼音进行索引化词库反查；24,323 条离线英文高频词、补充词与按前缀生效的稳定偏好排序；修复 Windows 双击词库查询入口；新增原生安装卸载入口；TSF 注册使用 DLL 内嵌 `π` 图标。

## 12. v0.4.0-dev 稳定运行架构

`v0.4.0-dev` 将 Windows 输入法拆成一个永久稳定的轻量 `PiInputTSF.dll` 和一个可独立重启、版本化安装的 `PiInputHost.exe`。Shim 只负责 COM/TSF 编辑、已确认组合状态镜像和有界本地 IPC；词库、全拼/小鹤解码、排序、英文候选、符号、设置与候选窗口全部由 Host 持有。

- 永久 CLSID：`{13EB305F-2DA3-4CF7-8C45-16B016B801B5}`；
- 永久 Profile：`{4ED27B7C-678E-4240-827A-24DA597F8D4B}`；
- 协议：`PIINPUT_IPC_V1`，固定宽度、有长度上限和序列/世代校验；
- 稳定 Shim：`%LOCALAPPDATA%\PiInput\Runtime\Shim\PiInputTSF.dll`；
- 版本 Host：`%LOCALAPPDATA%\PiInput\Runtime\versions\<版本>\bin\PiInputHost.exe`；
- 用户数据仍为 `%LOCALAPPDATA%\PiInput\UserData`；
- 普通升级排空旧 Host、原子切换路径、健康检查新版并支持失败回滚；
- 每个正式按键携带最新确认的组合恢复状态，Host 重启后继续输入；
- `piinput-diagnostics.exe` 报告实际 Profile、Shim 路径/哈希、Host 路径和实时健康状态；
- 开发期间禁止自动安装，最终 ZIP 由用户手动运行安装器验证。

英文候选继续默认关闭。用户开启后，Shift 英文状态使用随包的 24,323 词离线词频库和技术补充表；每次输入都把原始字母保留为首候选，随后按用户词、学习次数、基础词频、补全长度和稳定 ID 排序真正的前缀候选。至少输入三个字母后，候选不足时可追加少量有界近似补全，但不会压过真正的前缀词。用户提供的市场/科技英文段落已建立逐词自动补全回归。

代表性验收：

```text
hlheruhdlq / huangheruhailiu → 黄河入海流（首选）
无完整长句词条 → 按 = 进入分段取字 → 组合区暂存 → 最后统一上屏
```

详细行为见 `docs/词库查询与分段取字.md`。

## 12.1 v0.4.1-dev 文本光标定位

`v0.4.1-dev` 保持 `v0.4.0-dev` 的永久轻量 TSF Shim 与独立 Host 架构，并修正候选窗位置来源。Shim 在成功更新组合文本后，使用 TSF 的当前选择范围、活动视图与 `GetTextExt` 查询宿主程序真实文本插入点，通过独立、固定宽度的 caret 消息发给 Host。Host 先暂存候选快照，只有收到同一会话、同一 generation 的位置更新后才显示候选，杜绝候选先在鼠标处闪现再跳动。

- 正常程序：候选窗位于当前输入光标下方；
- 多显示器：允许负屏幕坐标，并按目标显示器工作区约束候选窗；
- 不提供文本几何位置的程序：显式标记位置不可用，继续使用原有鼠标附近回退；
- 过期、超前或其他会话的位置消息：拒绝，不移动当前候选；
- 紫色 `π` 图标继续内嵌于永久 `PiInputTSF.dll`，Profile 始终引用 DLL 内第一个图标资源。
- `v0.4.0 → v0.4.1` 的稳定 Shim 修订由安装器逐字节识别；未占用时原子刷新，被占用时中止且不切换 Host，禁止静默运行旧入口与新 Host 的不匹配组合。

## 12.2 v0.4.2-dev 稳定 Host 回归修复

`v0.4.2-dev` 以用户确认显示清晰、定位正常的 `v0.3.8/v0.3.9` 为候选 UI 回归基线。独立 Host 必须在创建任何窗口前启用 Per-Monitor-V2 DPI 感知，并在 `WM_DPICHANGED` 时按 `GetDpiForWindow` 重建字体和几何尺寸。TSF 插入点必须在成功写入组合文本的同一个编辑会话中取得；取不到时才允许使用鼠标位置。

Host 是当前用户唯一常驻进程，安装后立即启动并写入用户登录启动项。完整词库只在 Host 中加载一次，任何应用中的轻量 Shim 都只通过本地命名管道访问。Shim 丢失注册表指针时可从 `Runtime/current.json` 恢复 Host，不允许在每个按键或每个应用中重新加载大词库。

候选默认一行；`=`/下箭头按行展开，可靠普通候选耗尽后进入真实分段取字并高亮单字。左右箭头移动当前行候选，上下箭头移动候选行。独立 Shift 使用经过单元测试的 `ShiftToggleState`，禁止把 Shift 作为组合键时误判为模式切换。

Host 协议 v2 增加活动列、分段模式和完整组合区文本，仍兼容 v1 Shim。入口自身需要修正且已被应用占用时，安装器采用逐字节验证后的并行入口修订，新应用立即使用新版；普通 Host/词库升级继续不触碰入口 DLL。

## 12.3 v0.4.3-dev 极速常驻 Host 与独立测试台

`PiInput-Test.exe` 继续保持为不加载 `PiInputTSF.dll`、不注册输入法的独立 Win32 测试程序。顶部测试文本区是可自由编辑的多行文本框；候选提交到当前插入点。下方可切换中文候选和英文候选：中文复用现有 `Engine`，英文复用 `EnglishLexicon` 与 `EnglishSession`，并加载发布包内置词库以及本机可选 downloaded/user/learning 数据。日常词库和输入行为验证优先使用测试台，只有最终应用兼容性才使用系统 TSF。

Host 单实例锁必须在加载词库之前取得，禁止多个新窗口并发启动多个 Host、重复展开词库。Host 在发布健康状态之前预热全部 26 个首字母候选路径；稳定 Shim 的后台连接允许完整冷启动，并把首次中文字母排在握手之后，禁止首键漏成英文。默认 `resident-hot-v1` 只常驻 8105 通用字、Rime 通用词、诗词/成语、有效专业 SCEL 和用户词；完整 44k 单字与 411k 发音词源保留在外部目录，只有显式扩展构建才加入。真实进程级门禁覆盖冷启动、首个客户端、第二个新客户端和常见词命中。

候选窗在目标插入点所在显示器上先完成一次隐藏移动，再读取 `GetDpiForWindow` 并重建字体、尺寸和间距；不得使用窗口初始所在主屏的旧 DPI。125% 和 200% 缩放的 caret 间距、工作区约束、默认单行及真实 125% 屏幕截图均为 v0.4.3 验收内容。

## 12.4 v0.4.4-dev 稳定候选窗与可信分页

正常第一页仍按确定性分数展示最有用的整词或组合候选；`=`/下方向键只继续浏览连续的真实词库命中。下一行若只剩自动拼接句，不再展示多排无意义组合，立即进入当前未解决音节的单字/分段选择，已选文字留在组合区，最后统一上屏。

Host 收到新的候选快照时不得隐藏候选窗。它立即复用上一次文本光标更新内容；新应用首次无法提供 TSF 文本几何时，先使用系统 GUI 文本光标或鼠标附近的既有降级位置，后续真实 caret 只校正位置。相同内容不重复擦背景重画，解决每键 hide/show 引起的闪烁、消失和 Codex 无候选问题。

## 12.5 v0.4.5-dev 固定候选窗与可靠提交

同一次组合输入期间候选框固定左、上、宽和高，普通字母输入只更新文字内容。只有用户按 `=`、`-` 或上下方向键明确改变可见行数时允许改变高度；提交、取消或切换到另一应用会重置几何锁。首次只能取得鼠标降级位置时，允许首个真实 TSF 文本光标校正一次，之后不再跟随每键 caret 抖动。

稳定 Shim 在失焦时向常驻 Host 隐藏当前会话，重新获焦时通过 `ITfThreadMgr::GetFocus` 和 `ITfDocumentMgr::GetTop` 取得真实 `ITfContext`，并把 Resume 回复绑定到该上下文。空格或 Enter 提交先尝试同步 TSF 写入；宿主不允许同步写入时，自动切换到带完成回调的异步编辑会话。只有实际编辑成功才接受清空快照，失败时从 Composition mirror 恢复原始拼音、候选和 Host 状态。

## 11.1 v0.3.5-dev 基线

v0.3.5-dev 保留 v0.3.4 的安全延迟加载、完整基础词库、配置化输入、任意长度增量候选、可选离线英文候选和原生并存安装器。本版将用户确认的 406 个小鹤码固定为回归清单，使用外部 3500/7000 汉字表验证全拼和小鹤单字检索全部命中，并修正 `yuwh/yvwh` 兼容。候选窗去除方案标签。TSF 中文编辑在宿主拒绝编辑会话时恢复原快照，提交、取消和光标操作不再提前清空状态；安装器只加入键盘列表而不自动激活，避免安装动作影响当前应用和其他输入法。

外部目录固定为源码同级 `dicts/`，其中 `sources/` 保存自动下载仓库，`scel/` 和根目录兼容用户 SCEL，`user/` 保存自定义 TSV，`generated/` 保存统一 TSV，`cache/` 保存已验证二进制词库。`THUOCL` 会下载到本地，但默认不混入通用候选。

### 已实现并由 Windows Release 自动测试验证

- C++20/CMake 工程和完整文档体系；
- SCEL 元数据、拼音表、词条、权重解析；
- SCEL → TSV/JSONL/TXT；
- 标准普通话音节表；
- 全拼连续切分和 `'` 手动边界；
- 小鹤、自然码、微软、智能 ABC 双拼；
- 自有 `.lex` 格式和编译工具；
- 多 SCEL 批量导入和合并；
- 多词句子 Beam 解码；
- 基础词频和本地用户选择加权；
- 本地用户模型原子写入；
- 候选快照、候选唯一 ID 和过期候选拒绝；
- 中间插入、删除和基本光标移动；
- 中文、英文、程序员标点核心与 TSF 接入；
- 141 个符号及全部内置符号检索覆盖；
- CLI、词库工具、性能基准；
- Visual Studio 2026/2022 自动构建脚本；
- 根目录一键构建、测试、导入和开发安装；
- 内置起步基础词库；
- 基础词库与专业 SCEL 强制合并；
- `jisuanji` 和小鹤 `jisrji` 返回“计算机”的回归测试；
- Windows Release 全目标构建、真实 SCEL、大词库性能和完整 CTest 验证；
- `PiInputTSF.dll` COM/TSF 文本服务；
- 简体中文语言配置文件注册、启用和激活；
- `ITfKeyEventSink`；
- TSF Composition 创建、更新、提交和取消；
- GDI 最小候选窗口；
- 横向多行候选，Space、数字键、行滚动和翻页；
- Backspace/Delete/Home/End/左右移动；
- Enter 原始拼音提交、Esc 取消；
- TSF 内符号搜索；
- 中文和英文本地选词学习，英文多进程增量合并；
- 英文候选默认关闭，支持事务词库更新和稳定 uint32 来源标志；
- `u/v` 兼容和任意长度未完成编码候选；
- 用户确认的 406 个小鹤码全部可解码；
- 3500 常用汉字和 7000 通用汉字的全拼、小鹤检索均为 100% 覆盖；
- TSF 编辑会话失败保持组合输入和候选快照；
- 安装器不自动激活 PiInput，由用户在目标应用中通过 `Win + Space` 选择；
- `PiInput-Install.exe` 根目录双击安装、版本并存和 UAC 单次授权；
- 无品牌硬编码的旧运行环境发现、用户数据事务迁移、冲突副本、失败回滚；
- 锁定旧 DLL 的重启删除计划；
- `piinput-profile.exe` 方案设置和配置文件激活；
- 注册、修复、验证和卸载脚本；
- 改进后的独立 Win32 预览；
- `docs/安装与使用指南.md` 与 Windows x64 用户发布 ZIP。

### 尚未完成，不能声称已完成

- Visual Studio、VS Code、Chrome、Office、微信、Terminal 等应用兼容；
- 32 位宿主应用和 Windows ARM64；
- DirectWrite/Direct2D 候选窗；
- 精确 TSF 插入点定位、高 DPI、多显示器和深色模式；
- 状态栏和设置图形界面；
- 已选词与未选拼音混合编辑；
- 按音节移动、非法拼音高亮、Tab 切分；
- 高质量通用基础词库、单字词库和二元语言模型；
- 词库分层权重、置顶、屏蔽和错误学习管理 UI；
- 完整符号面板、收藏、最近和自定义；
- 设置程序；
- 独立引擎进程与 IPC；
- 正式代码签名与商店级安装体验；
- macOS、Android、iOS；
- 跨设备同步。

## 12.6 v0.4.6-dev 多窗口输入可靠性

- 一个宿主进程内的不同 TSF 文本 Context 使用独立 Host 会话和组合镜像；
- 新 Context 不继承旧标签页的拼音，旧会话迟到回复不能移除新会话的等待项；
- TSF 暂时拒绝同步更新时，候选与组合文本使用异步编辑会话补做；
- Shift 回调缺少 Context 时主动查询当前焦点，保持中英文切换可用；
- 新会话等待一次 TSF caret 结果后再显示，避免先在鼠标处闪现再跳到插入点；
- 候选布局按文字长度自适应为 6、2 或 1 列，长句不再显示成重复的“我…”；
- 同一组合锚点固定，窗口宽度只允许按需要增大，短候选不会使窗口来回缩放；
- 中文状态下 `--`、`==`、`++` 保持 ASCII，Shift+- 仍输入中文破折号；数字后的句点按 ASCII 小数点/列表点处理。

## 12.7 v0.4.7-dev Shift 丢失 KeyUp 恢复

- 单独按 Shift 仍在正常 KeyUp 到达时立即切换中文/英文；
- 某些 TSF 宿主漏发 Shift KeyUp 时，下一次普通按键会检查 Shift 的真实物理状态，并在处理该键前补做一次切换；
- Shift 仍被物理按住并与其他键组合时，只按修饰键处理，不误切换输入模式；
- 迟到的 Shift KeyUp 不会造成第二次切换；
- 英文候选关闭时，Shift 仍可进入英文直输，字母和英文标点立即上屏且不弹候选；
- 英文直输状态在独立 Host 重启或断线恢复后保持不变。

## 12.9 v0.4.9-dev 中文符号、符号入口与候选窗口

- 中文符号默认切换为搜狗常用映射，并允许 `bracket_style=wechat` 使用 `「」`；
- 新增 `;;f`、`；；f` 与双反引号加 `f` 的等价符号中心入口；
- 单反引号提交 `·`，三反引号 Markdown 标记不被命令路由吞掉；
- 候选窗口最大宽度为 600 DIP，长标题水平裁剪但不显示省略号；
- 普通候选窗不再重复绘制 Composition 编码标题，折叠状态固定为 48 DIP；第一页按候选顺序和实际宽度预算尽量填满，不再被后排某个长候选粗暴降列；
- 候选窗口定位以宿主当前选择的真实插入点为主，Composition 末端只作兼容回退；跨进程坐标按宿主 DPI 感知转换为物理屏幕坐标，避免不同编辑器和缩放比例下出现整段拼音宽度的横向偏移或不一致的垂直间距；
- 4/6/8 字及长句候选自动减少列数，默认仍为一行并按 `=`/方向键逐行展开；
- 本轮只修改本地工作区，不推送远程仓库。

## 12.10 v0.5.0-dev 候选栏工具与外观设置

- 候选栏默认采用 48 DIP 高度、16 DIP 字体、淡紫圆角首选和浅灰自绘边框；

- 候选栏右侧增加不占候选编号的向下展开按钮和固定四格工具按钮；
- 工具菜单当前只提供“符号”和“设置”；
- “符号”直接路由到当前客户端、当前会话的符号中心；
- 新增原生 `PiInput-Settings.exe`；
- 候选文字大小可设为 10～28，单行高度可设为 20～72，文字始终垂直居中；
- 设置程序可选择全拼或小鹤双拼，并可选择默认进入中文或英文状态；
- 字号和候选栏高度支持鼠标滚轮逐格加 1、减 1；设置页预览与真实候选栏均使用明确的垂直居中绘制；
- 设置原子写入 `%LOCALAPPDATA%\\PiInput\\UserData\\settings.ini`；
- 视觉设置只在下一次组合边界热更新，不重启 Host、不重载词库；
- 安装包必须同时包含并验证设置程序。

## 12.11 v0.5.1-dev 紧凑高度与跨屏定位修复

- 候选栏默认高度由 48 DIP 调整为 40 DIP，16 DIP 字体继续在首行垂直居中；
- 每监视器 DPI 感知应用不再重复进行逻辑坐标到物理坐标转换，避免候选栏偏向屏幕右侧或落到另一台显示器；
- 候选窗始终在目标显示器工作区内保留 4 DIP 安全边距，边框不会贴着屏幕边缘被裁切；
- 最终上屏先结束 TSF Composition，再进行可选的光标定位，避免 Notepad++、Word 等文本存储拒绝光标调整时丢失已选文字。

## 12.12 v0.5.2-dev 增量候选排序修正

- 对未完成的小鹤编码按“当前尾部前缀”分配更高但有界的词库扫描预算；
- 未完成输入 `rug` 的第一页固定优先显示“如果、入股、乳沟”，不再被“如鲠在喉”等更长词抢占；
- 保留完整长词优先：`hlheruhdlq` 和 `huangheruhailiu` 仍以“黄河入海流”为首选；
- 对额外补全的音节和重新打开的已完成音节分别施加确定性惩罚，减少无意义长组合；
- 任意长度的尾部未完成编码仍可继续补全，`biruwoycuibuv` 的候选中包含“比如我要是不知道”；
- 增量扫描总预算不增加，真实 101,469 词词库的普通热查询 P95 保持在 0.11 毫秒以内，长未完成句 P95 约 0.51 毫秒；
- 本轮只修复排序和测试，不增加 AI、云联想或新的输入功能。

## 12.13 v0.5.3-dev 候选质量与本地控制

- 安装升级刷新 TSF Profile 图标元数据，稳定入口身份不变。
- 中文候选使用微软雅黑 UI 与显式垂直基线。
- 句中语气词与扩展区生僻字采用可解释的确定性降权。
- 候选右键支持固定首位与本地屏蔽，状态写入用户模型。

## 12.14 v0.5.4-dev 组合取消与失焦恢复

- 普通失焦保留仍在途的 TSF 编辑请求，同一文本上下文不会重复 Resume。
- 应用主动终止组合时立即清空镜像并拒绝终止前的迟到回复，避免截图、点击输入框或焦点返回后重复原始字母。
- 候选右键菜单按 `Esc` 或点击外部时，精确取消对应进程与会话的 Host 和 TSF 组合。
- 无真实候选的原始输入不再显示空白候选框。
- 小鹤 `mktm` 首选“明天”，按 `=` 后进入当前音节的单字选择。

## 12.15 v0.5.5-dev 候选分页与本地用户造词

- `=`/`↓` 和 `-`/`↑` 按候选行稳定移动，新行始终选中第一项；
- 先展示完整真实词语，正常候选耗尽后才进入长词段优先、单字兜底的分段取字；
- 分段结果统一上屏，只有 TSF 实际提交成功才写入学习；
- 一次学习进入第一行，二至三次逐步前移，普通学习词最多保留两个第一行位置；
- 全拼和小鹤共享标准拼音用户词；
- 固定首位、取消固定、删除候选和屏蔽 tombstone 均保存在本地；
- Host 内存学习跨窗口立即可见，旧候选 generation 不后台跳变；
- 用户模型由 Host 约 1.5 秒防抖批量、原子保存，按键热路径不写盘；
- Host 协议升级到 v3，shim 只在编辑会话实际完成后报告提交结果。

## 12.16 v0.5.6-dev 稳定入口与热切换修复

- 全拼/小鹤设置在组合边界热更新时保留会话 generation 单调递增，已经打开的应用不再吞掉下一次字母；
- Windows COM 永久入口只允许指向 `%LOCALAPPDATA%\\PiInput\\Runtime\\Shim\\PiInputTSF.dll`，稳定 Shim 被应用占用时不再退回注册版本目录中的 DLL；
- 安装器等待旧 Host 完成排空，并核对新 Host 健康响应中的 build ID，旧进程不能再冒充新版本安装成功；
- 诊断工具初始化 COM 后再查询 TSF Profile，注册、启用和激活状态与 profile 工具保持一致；
- 安装包仍把 Host 和数据放入并行版本目录，通过原子 `current.json` 切换当前 Host；
- 候选窗定位传递完整 TSF 文本光标矩形，屏幕底部向上翻转时使用光标上边缘，避免 Word 和多屏工作区裁切；
- 候选窗仍只在新组合、显式展开或真实文本光标替换临时鼠标锚点时改变几何位置，普通输入过程中保持稳定。

## 12.17 v0.5.7-dev 候选队列、一次学习与长输入延迟

- `=`/`↓` 按行向下移动，连续按时滚动可见窗口，最早一行不固定、可以离开视野；
- 多行候选仅当前选中行显示数字，其他行只显示候选文字；
- 所有可信真实词语先按稳定顺序展示，词语耗尽后才追加分段取字和单字；
- 分段取字与原整词共用同一候选快照，不再用单字候选覆盖原候选；
- 展开行数继续由 `visible_rows` 配置，默认 3，支持 3～6 行验收；
- 用户分段组成的新词在一次成功提交后立即学习，失败、取消与过期提交不记录；
- 基础词库加入“开悟”“丝滑”，真实二进制词库全拼/小鹤首选均通过；
- 长输入根据完整音节数限制 Beam 宽度，真实 101,472 词词库长全拼 P95 从约 15.9 ms 降到约 2.5 ms；
- 新增长全拼大词库性能门禁，P95 5 ms、P99 10 ms，避免后续重新引入逐键卡顿。

## 12.18 v0.5.8-dev 连续输入传输延迟

- 根因不是拼音解码或词库查询，而是 Host 每处理完一个短连接后才创建下一条命名管道；连续按键到达时会轮询等待，周期性产生约 15～20 ms 停顿；
- Host 现在处理当前请求时提前创建一个待连接的下一管道实例，下一按键无需等待入口重建；
- 当前响应仍在断开前确认客户端已经读取，兼顾 32 位/64 位启动路径的可靠性；
- 新增 80 次连续 Host 传输 P95 小于 5 ms 的门禁，并在 32 位和 64 位 Windows 路径验证无丢包；
- 真实 101,472 词二进制词库加入“我们明天、去哪里、这个是、你觉得、怎样”的逐键延迟回归；
- 真实词语按键门禁为 P95 小于 10 ms、单次最大值小于 20 ms，连续运行五轮必须稳定通过。

## 12.19 v0.5.9-dev 标点提交时序

- 修复快速“中文→标点→中文→标点”时多个 TSF 最终编辑相互覆盖的问题；
- 最终编辑未完成时后续按键有序排队，每次回复或提交边界只放行一个；
- 切换文本上下文、外部终止组合和停用输入法时清空旧队列；
- 正常连续字母输入继续走异步快速路径，不增加常态按键等待。

## 12.20 v0.6.0-dev 有界冷启动与断线熔断

- 用户实录确认固定约五秒的冻结来自稳定 TSF 入口的 Host 冷启动等待，而不是拼音解码或词库查询；
- 删除每个断线请求都可触发的 5000 毫秒等待，首次连接失败只允许一次 750 毫秒冷启动预算；
- 同一故障窗口内后续按键快速失败，不再堵塞单线程 FIFO 并在五秒后批量补字；
- 1750 毫秒冷却结束后允许一次新的有界重连，连接恢复后自动重置熔断状态；
- 当前机器完全停止 Host 后到健康响应实测约 357 毫秒；
- 新增断线、冷却、恢复和禁止五秒等待的自动回归门禁。

## 12.21 v0.6.1-dev 逐键延迟与标点后输入

用户实录：连续输入「我，他们。我们明天，去哪里？明天、后天！打后天。」整体偏卡，标点之后再输入文字最明显。本版只做性能与热路径修复，不增加功能，不改变候选内容。

已定位并修复的四类真实开销：

1. **每条 IPC 消息分配 1 MiB 协议上限缓冲区。** 稳定 Shim 与 Host 双方每次读管道都新建并清零 `host_header_bytes + host_max_payload_bytes`（约 1 MiB），而真实回包只有几 KB。改为各自持有一个 16 KiB 起步、按 `ERROR_MORE_DATA` 增长且不再缩小的复用缓冲区。空载往返 P95 由约 809 微秒降到约 71 微秒。
2. **组合边界在按键热路径上做文件系统探测。** 每次组合为空的按键（即每个词的首字母，也包括每个标点之后的第一个字母）都会复制一份完整候选快照仅为判断 `raw` 是否为空，并对 `settings.ini` 做两次 `std::filesystem` 查询；设置文件不存在时每次还会抛出并捕获一次异常。改为 `SessionManager::at_composition_boundary()` 直接判空，并用一次 `directory_entry` 刷新配合 `error_code` 完成非抛出探测。这是「标点后再输入更明显」的直接原因。
3. **Host 解码自己刚编码的回包。** 候选展示需要 `HostReply`，此前是把刚写出去的 payload 再解析回来，等于每键多做一次全量候选字符串解码。`SessionManager::dispatch` 现在可直接输出该回复。
4. **句子解码在真实候选页大小下的开销。** 基准工具原先固定按 10 条候选测量，而 Host 实际请求 90 条（`CandidateSettings::max_items`），两者相差约 10 倍，性能门禁因此长期没有覆盖真实热路径。基准新增 `--limit` 且默认 90；解码器增加两处优化：进入定长 beam 前先用整数比较判定能否入选，避免为必然淘汰的路径复制句子文本；未完成音节的补全阶段把「已消费音节前缀」提到路径与词的双重循环之外。同时将补全词与句子前缀的终端组合预算限定为 8 条（句子 beam 本身不变，分词质量不变）。

其他热路径清理：候选栏每键只重建一次 UTF-16 候选列表（此前暂存快照与真实光标各做一次），几何未变化时不再重复 `SetWindowPos` 与窗口区域重建；`candidate_items_per_row` 只接收前若干列，不再为每键复制最多 90 条候选词；`encode_host_reply` 预先按候选规模预留缓冲区；`FinalEditKeyQueue` 增加 64 键上限。

实测（真实 101,472 词二进制词库，本机 Release）：

- 空载 IPC 往返 P95：809 → 71 微秒；
- 逐键 Host 往返 P95（`womfmktm`）：2582 → 1417 微秒；
- 词—标点—词链路 P95：2484 → 1446 微秒；
- 引擎查询（`limit=90`）：`wom` 590 → 197 微秒，`womfmkt` 1191 → 341 微秒，`womfmktm` 398 → 74 微秒，`biruwoycuibuv` 约 1880 → 610 微秒；
- 整句 41 次按键：P50 78 微秒，P95 1405 微秒，标点后首字母 P95 121 微秒。

候选质量回归全部保持不变：`rug` 仍为如果/入股/乳沟，`mktm` 仍为明天，`womfmktm` 仍为我们明天，`qvnali` 仍为去哪里，`hlheruhdlq` 与 `huangheruhailiu` 仍为黄河入海流，`biruwoycuibuv` 候选中仍包含比如我要是不知道。

新增门禁：

- `piinput-keystroke-latency` 用真实词库重放上述整句，逐键测量并单独统计标点后首字母，P95 上限 2 毫秒、单键上限 6 毫秒；
- Windows 源码回归禁止两端重新出现每消息 1 MiB 缓冲、禁止 Host 重新解码自身回包、要求组合边界使用非抛出探测；
- Host 进程门禁收紧：空载往返 P95 由 5 毫秒收到 2 毫秒，逐键 P95 由 10 毫秒收到 6 毫秒、单键上限由 20 毫秒收到 12 毫秒；
- `piinput-benchmark` 默认按 90 条候选测量，两个外部大词库性能门禁因此真正覆盖生产页大小。

## 12.22 v0.6.2-dev 英文直输与候选保留

用户反馈两件事：按 Shift 切到英文后打字特别慢；按 `=` 之后原来的整词候选整行消失，被逐字候选顶替。本版只修这两条，不改排序权重。

### 英文直输每个字母都在建毁一次 TSF Composition

英文候选默认关闭，此时 Host 对每个英文字母返回 `commit`（`host_session.cpp` 的 `mode_ == english && english_ == nullptr` 分支）。稳定 Shim 收到 commit 后走 `apply_composition_edit`，而当时没有进行中的组合，于是它 `StartComposition` → `SetText` → `EndComposition` → `SetSelection`，等于**每敲一个英文字母都让宿主程序完整经历一轮输入法状态切换**。在 Word、浏览器、Electron 这类程序里，这四步各自都要跨边界通知，累积起来就是明显的迟滞。

实测确认瓶颈不在 Host：英文直输的 Host 侧逐键 P50 为 181 微秒、P95 为 408 微秒，与中文同级。

修复：commit 到达时若没有进行中的组合，改用 `ITfInsertAtSelection::InsertTextAtSelection` 直接在插入点写入，然后把光标折叠到文本末尾。一次操作取代四次。该路径同时覆盖组合区为空时单独输入的中文标点。宿主拒绝移动自身光标不再被当作提交失败。

### 按 `=` 会顶替掉正在看的整词候选

`ImeSession::refresh_segment()` 此前只保留 `normal_browse_candidate_count` 条整词，而该计数是「候选表开头连续可信条目」的长度。像 `kady`、`fwihkk` 这种第一行全是真实词但只有第一条算可信命中的情况，该计数是 1 甚至 0，于是按 `=` 之后整行真实词被逐字候选顶替。

修复：`enter_segment_selection()` 接受「用户当前已经看到的整词条数」，即 `(当前行 + 1) × 每行条数`；`refresh_segment()` 保留两者中的较大值。分段取字从此始终追加在已见词行的下方，长词段优先、单字兜底的既有顺序不变。真实词库下 `kady` 按一次 `=` 的结果为：

```text
第 1 行：卡吨 卡顿 卡盾 卡蹾 卡炖 卡蹲     ← 原词行保留
第 2 行：卡 喀 咖 咔 佧 咯                 ← 分段取字追加
第 3 行：垰 胩 鉲 擖 裃
```

### 英文直输的跨进程往返

去掉建毁组合之后仍有迟滞：每个英文字母仍要跨进程往返 Host 一次，再由后台线程回投到应用消息循环才落到文档。英文直输既无组合也无候选，Host 只是原样回传，这一趟没有必要。Shim 现在用「切到英文后第一个字母的回复是否为单字符提交」判定直输模式（英文候选开启时该字母返回组合更新，判定自然为否），其后的字母在 `OnKeyDown` 内同步写入，不再联系 Host；有请求在途时退回原有顺序路径，避免字母抢在待提交标点之前。模式切换、上下文重绑与停用都会重置该判定并重新探测。

### 词库缺词与 SCEL 膨胀

`kady` 首选是「卡吨」而非「卡顿」，根因是**两者都不在词库**：排序只能由单字权重决定（吨 2100 > 顿 1820）。「卡顿」仅出现在 `phrase-pinyin-data` 这一只提供读音、权重统一为 1 的源，按既定规则不得混入常驻词库，默认 `resident-hot-v1` 将其排除；它同时不在 Rime 通用词、随包 SCEL 和内置起步词库中。「补全」连读音表都没有。两词已按 `ka'dun`、`bu'quan` 加入 `data/base_lexicon.tsv`，并在已发布基线上重新编译，条目数 101,472 → 101,474。

本轮同时发现 `dicts/` 的 SCEL 由发布时的 11 个增至 24 个，另有 12.7 万条的 `dicts/user/网络流行新词.tsv`（按权重 60000 合并）。按现状整库重建得到 40 万～52 万条，并使长句验收「固件开发需要熟悉底层寄存器配置和链路状态机」失败，正是「冷门专业词污染普通候选」被门禁抓住。本版因此不整库重建，只在已验证基线上追加这两个词。

新增回归：`kady` 场景在 `host_session_tests` 中固化为「按 `=` 必须保留整行可见词并把逐字候选追加在下方」。`fwihkk` 原有断言此前锁定的是被顶替后的列表（首项 `非场`），现已改为校验 `非常快` 整行保留、追加段以真实长词 `非常` 开头。Windows 源码回归新增一条：没有进行中组合的提交不得再创建并结束 TSF 组合。

## 12.23 v0.6.5-dev 分段取字前进与候选栏对齐

用户对照搜狗提出四项，本版只做这四项，不改排序权重。

1. **分段取字选字后停在原地。** `HostSession::choose()` 暂存一个字后执行 `advance_generation(true)`，其中的 `candidate_grid_.reset()` 把选择位置归零；v0.6.2 引入「保留已见整词行」后索引 0 变成整词，于是选完一个字落回整词行。新增 `select_first_segment_candidate()` 收敛定位逻辑，进入分段取字与暂存一个字都用它，选择位置落在刚空出来的音节上。
2. **候选框默认五行。** `visible_rows` 默认 3 → 5，同步内置默认设置、原生安装器写入的 `settings.ini`、`install-dev.ps1`、`set-candidate-page-size.ps1` 与 `CandidateVisualSettings`。已有 `settings.ini` 中的显式值仍然覆盖默认。
3. **候选编号位固定预留。** 编号字符串在所有行都构造并测量，`item.left` 一律前移编号宽度，只有活动行绘制编号；列宽测量也一律按含编号的文字进行。此前非活动行的编号串为空、宽度为 0，导致上下行文字错位。
4. **标点后再输入的延迟。** Host 侧实测空载往返 P95 199 微秒、中文—标点—中文链路 P95 1281 微秒、普通词逐键 P95 1463 微秒，标点边界并不比普通按键慢，剩余延迟在应用侧 TSF 路径；词边界固有多出的 `StartComposition` 无法省略。本版去掉候选窗的重复测量：每个按键会定位两次（暂存快照、真实光标），窗口几何已锁定且内容与可见行数未变时第二次只可能得出同一矩形，现在直接复用锁定矩形，跳过设备上下文与逐项 `GetTextExtentPoint32W`。默认五行后每次测量最多 30 项，该项收益随行数增加。

5. **提交之后第一个字母的候选栏要等一个来回。** 词中间 `stage()` 的 `reuse_caret` 为真，按键回复到达即显示候选栏；提交后 `presenter_->hide()` 清掉 `focused_session_` 与 `caret_available_`，下一键 `current_caret()` 返回空，`stage()` 直接返回不显示，候选栏要等 Shim 完成 `StartComposition` → `GetTextExt` → caret 回程。`CandidatePresenterModel` 现在跨 `hide()` 记住最后一次真实光标，下一次组合用它作临时锚点立即显示，并以 `show_at_provisional_caret()` 保持 `locked_to_text_caret_` 为假，使第一个真实光标按既有规则校正一次。该现象与标点无必然关系，任何提交之后都相同。

新增回归 `test_staging_a_character_advances_to_the_next_syllable`、`test_first_key_after_a_commit_reuses_the_previous_caret`；`test_candidate_screen_size_uses_fixed_dimension_fallback_priority` 的数值按新默认重新设计，回退顺序与错误条数不变。

## 12.24 v0.6.5-dev 提交后按键不再等待异步编辑

用户在 v0.6.4 上录屏复现「打完标点继续输入卡顿」。30 fps 逐帧哈希比对显示：候选栏与组合区文字已同步（差 1～2 帧），但提交之后的第一个字母从未单独成帧，与第二个字母同帧出现；词中间的字母则逐帧单独出现。慢的是组合区文字本身。

根因：`FinalEditKeyQueue::should_queue()` 只要 `final_edit_pending_` 为真就拦下所有按键，而该标志一直保持到提交的 TSF 编辑真正完成。`handle_reply()` 的提交分支在 `request_edit` 返回 `pending` 时不调用 `complete_final_edit()`，要等 `complete_deferred_edit()`，也就是应用下一轮消息循环授予异步编辑会话之后。Notepad++ 经常拒绝同步编辑会话（v0.4.6 的异步回退即为此），于是提交后的按键被逐个放行，每个一次完整往返。

修复：新增 `key_may_begin_final_edit(kind, english_mode)`，只有回复可能再次提交或取消的按键才需要等待挂起的提交；普通汉字字母与候选导航立即放行，其 TSF 编辑仍由 `DeferredUpdateQueue` 排在提交之后并合并为最新一次。队列非空时一律排队以保持顺序。

录像无法给出按键物理时刻，因此绝对延迟无法直接测得，判据是「提交后首字母是否单独成帧」。

## 12.25 v0.6.6-dev 粘贴内容不再被组合终止清空

用户报告：在 Claude、Codex 这类空文本框里粘贴内容后立刻打字，粘贴的内容会被整段删除。

根因在 `TextService::OnCompositionTerminated`。v0.5.4 为了避免「点到别处时编辑器把生拼音留成正文」，在这里对组合范围无条件执行 `SetText(edit_cookie, 0U, L"", 0L)`。这类基于 contenteditable 的 Web 编辑器在粘贴后会重建文档并终止组合，此时那个 `ITfRange` 已经映射到用户刚粘贴的文字上，于是被整段清空。

修复：只擦除本输入法确实写进去的内容。新增 `range_holds_exactly()`，用 `ITfRange::GetText` 多读一个字符与 `CompositionMirror` 记录的组合文本逐字比较，完全一致才清空；范围更长、更短或读取失败一律不动。残留一个拼音音节远好于删掉用户的粘贴内容。

同版第三处修复是候选框弹出位置。用户的判据是「在满屏文本的 Notepad++ 里随便点一个位置直接打字，看第一次弹出的候选框在不在插入光标下面」。

根因是 `CandidatePresenterModel::stage()` 里 v0.6.1 加的「继承上一个词的光标位置」快捷路径：它假设下一个词就在上一个词右边几个字符处，用户点到别处时候选框会先弹在上一个词结束的位置。

修复分三层：Shim 在每个词的**第一个按键**上先用 `TF_ES_SYNC | TF_ES_READ` 只读会话探测当前选区位置并发给 Host（`CaretProbeSession` / `probe_selection_caret()`）；探测不到时明确发送 `has_text_caret = false`，Host 清掉旧锚点并等待真实光标，绝不猜位置；`anchor_locked_` 让锚点在一个组合之内固定，不跟着右移的插入点漂移。权威锚点仍然来自成功的组合编辑会话，v0.4.2 的约束没有放松，源码门禁改为同时断言「组合会话内取值保留」与「只读会话仅用于按键前探测」。

同版修复中文模式下的减号。`-` 原本无条件映射为候选行上翻，没有可翻的行时变成死键，导致「in-to」中间的连字符打不出来。现在 Shim 在 `previous_row` 上带出字符 `-`，Host 在无行可翻且不处于分段取字时按普通标点处理：先提交当前候选，再送出连字符。方向键上不带字符，仍是纯导航键。

## 12.26 v0.6.9-dev 不覆写用户文字与候选框只在真实光标处出现

v0.6.6 宣称修复的两个问题在真机上都没解决，两段录屏经进程启动时间与模块核对，确认跑在真正的 v0.6.6 上。

**粘贴内容被删除**：v0.6.6 判定根因为 `OnCompositionTerminated` 的无条件清空，属于误判——第一个按键时组合尚未建立，那条路径不会执行。真正的路径是 `apply_composition_edit` 里 `range->SetText()` 覆盖整个组合区范围；contenteditable 编辑器在粘贴后重建文档，交回的范围可横跨用户粘贴的文字。修复：新增 `range_text_equals()` 与成员 `composition_written_`，写入前校验范围里装的确实是上次自己写入的内容（新组合应为空）；不一致时结束该组合并在当前选区重开，仍不为空则放弃这次按键。丢一个按键远好于毁掉用户内容。

**候选框位置**：`anchor_locked_` 是 v0.6.6 引入的回归。词打开时先绘制「记住的锚点」这一猜测，而绘制同样经过 `apply_caret()`，于是猜测把锁设上，一个往返后到达的权威光标被丢弃，候选框整词停在错位置。修复：新增 `provisional_pending_`，`anchor_locked_` 只能由权威光标设置；锁的释放条件改为「不是复用已显示的锚点」，防止跨词残留。

**打包应用无法加载 Shim**：ChatGPT/Codex 桌面版位于 WindowsApps 目录，是 MSIX 打包应用，运行在 AppContainer 内。AppContainer 只能读取授予 `ALL APPLICATION PACKAGES`（S-1-15-2-1）的文件，而 Runtime/Shim 下的 `PiInputTSF.dll` 没有该权限（搜狗的 `SogouTSF.ime` 有），因此 Shim 从不被载入，输入法在这些应用里连切换都切不过去。安装器新增 `grant_app_container_read()` 授予该权限，字节未变的安装路径也会执行以修复既有安装。

**候选框「总是差一步」**：所有猜测锚点的手段全部移除，候选框只在组合自身编辑会话取得的真实光标处出现，宁可晚一个消息循环回合。

**候选框定位**：新增独立工具 `piinput-caret-demo`，用生产 `CandidateWindow` 类配合直接读取的光标，把「光标来源」与「何时允许移动」拆开验证。44 个采样全部 `dx=0`、`dy_below_caret=5`，证明定位数学无误，问题在窗口层几何锁只由 `hide()` 的副作用释放。改为显式释放：`release_anchor()` + `word_just_opened()`，组合打开时取锚点、打字期间冻结、上屏后释放。

流程教训：先写复现测试再改代码；改生产代码之前先用独立工具把结论测出来。`test_a_provisional_anchor_never_blocks_the_real_caret` 在修改前确实失败。另外 `package-release.ps1` 现在会比对 `dist/` 与 `build/` 的二进制哈希，防止再次发布陈旧产物。

## 12. 下一阶段

`v0.7.12` 已具备可日用的 Windows x64 系统输入、离线大词库、稳定 Shim/Host、原生设置与安装升级基础。下一阶段仍不扩展平台，优先：

```text
真实密码框矩阵验收
→ 许可证和词库来源闭环
→ Windows CI、代码签名和干净发布包验证
→ 补齐 13 个专业词已知缺口并建立首选率黄金集
→ 8 小时稳定性、句柄和内存曲线
→ 优化大词库冷启动和私有内存
```

隐私、发布合规、准确率和长期稳定性完成以前，不开发跨设备同步和移动端。

## 13. 每版发布包硬性要求

每个发布包必须包含：

- 完整源码；
- `PROJECT_CONTEXT.md`；
- 架构、任务、约束；
- 版本说明；
- 带目标版本号的下一版计划；
- 新会话续接说明；
- 构建、测试和安装说明；
- 实际验证记录；
- Git 命令；
- `VERSION`、文件清单和 SHA-256 清单。

不得只交付差异文件。源码开发包不得携带旧 `build/`、`dist/`、`.vs/`、二进制和 CMake 缓存。
## 12.27 v0.7.0 单向提交回执与数字 0 小数点规则

- 真机两端追踪确认，标点后约 0.5 秒停顿不在 TSF 编辑、应用消息循环或 Host 计算，而是 `commit_result` 成功送达且按协议静默时，被 Shim 当成交换失败并进入 750 毫秒冷启动重试；
- `ShimPipeTransport` 现在区分“请求已成功写入”和“收到有效回复”。单向 `commit_result` 只要成功写入即完成，真实未送达仍沿用 Host 拉起和重连；
- 数字直通上下文使用 `0` 到 `9`，不再复用仅服务候选选择的 `1` 到 `9` 判断；
- 中文模式下 `0.7.0` 保持 ASCII 点，`0..` 输出 `0.。`，第二个点不会错误继承数字上下文。
- 登录启动项直接运行控制台 Host 会弹出黑窗口；v0.7.0 安装器删除旧 `PiInputHost` 启动项，安装时和 Shim 按需拉起都使用 `CREATE_NO_WINDOW | DETACHED_PROCESS` 静默后台运行。
## 12.28 v0.7.1 Rime Ice 公共词库与词法候选

- 发布用中文候选只从 Rime Ice 主词典 `rime_ice.dict.yaml` 声明的默认表导入：`8105`、`base`、`ext`、`tencent`、`others`，按声明顺序先声明者胜；
- 项目自造的精选表、`rime-pinyin-simp`、THUOCL、SCEL、`pinyin-data`、`phrase-pinyin-data` 和 `dicts/user/*.tsv` 都不再混入发布词库，发布包只携带编译后的 `data/piinput-base.lex`，不再携带 `base_lexicon.tsv`；
- `Engine` 改为直接做词法跨度查询，不再把 `IncrementalDecoder` 的整句路径当成普通候选发布，不机械拼句；
- 候选按显示文字去重，真实多字词全部排在首音节单字兜底之前，不为单字保留固定名额；
- 只有应用确认最终提交成功后，整词、组成部分和相邻片段才写入个人学习层 `%LOCALAPPDATA%\PiInput\UserData\user_model.tsv`。
## 12.29 v0.7.2 真实词拼接与全拼前缀分词修正

- 整串输入全部解码、但没有单条词库条目覆盖它时，引擎在主 parse 上做一次有界动态规划，把真实多字词拼接成覆盖全部音节的候选排在最前，最多两条；
- 拼接跨度上限 8 个音节，每跨度取前 4 条、每位置留 4 条路径、最多 4 个词，每一段都必须是真实多字词，单字不得作为衔接；
- 拼接排序按词数少、最弱环词频、总词频依次比较；`EngineCandidate::score` 带上总词频，否则外层排序在 `base_weight` 打平后退化成字典序；
- `Engine::query` 排序新增「词数少者优先」，单条真实词库条目压过同样覆盖的拼接候选；
- `src/pinyin_prefix.cpp` 的 `sort_and_limit` 原本以「完整音节数降序」为主键，压过了分词器自己的评分，全拼下结尾带半个音节的输入全部选中碎分词并漏词；现在改为未解析字母数升序、分词器评分降序、音节数升序、字典序定序。
## 12.30 v0.7.3 逐键拼接：三音节兜底、单字收尾与未完成音节

- 现象：小鹤 `xpzdw`→`xpzdwj`→`xpzdwji` 连续三次按键候选完全不变，直到 `xpzdwjig` 凑出「卸载完成」才跳变。用户在这三键期间拿不到任何反馈，打错也看不出来；
- 根因一：`compose_full_coverage_unlocked` 入口是 `count < 2*2` 直接返回，三音节（`xie'zai'wan`）从未进入拼接。三音节的拼接必然是「2 音节词 + 1 音节字」，绕不开单字；
- 根因二：每段至少 2 音节且拒绝单字条目，「卸载」+「完」这种形状永远拼不出来；
- 根因三：未完成音节只走 `query_completions_unlocked` 的整词补全，补全上限是 `complete + 1` 个音节，而「卸载完」不是词条、「卸载完了」是 4 音节被上限挡掉，于是尾部半音节 `w` 等同被忽略；
- 修复：拼接接受 `trailing_prefix`，把「已确定的词 + 正在输入的音节」并入同一套有界动态规划。单字可以收尾，但必须由多字实词打底、至多一个、且只能落在输入末尾；`卡`+`吨` 这类无实词打底的字串联仍然被拒，v0.6.2 的「卡吨」问题不会复现；
- 未完成音节的补全池只按词频排序时，「我」会压过「完」。改为先查一次 `canonical_prefix` 的长词前缀（如 `xie'zai'wan'le`），取出该音节的真实读法作为上下文证据优先，因此半音节 `w` 就能给出「卸载完」；
- 拼接的意义是覆盖没有单条词条能覆盖的输入，所以词库一旦有整词命中全部音节（`我爱你`、`你好吗`），拼接直接关闭，避免「我爱尼」挤掉「我爱」；
- 拼接候选数：未完成输入至多 3 条，完整输入至多 2 条；
- 这条改动推翻了 12.28 的「不机械拼句」的一部分：实词加一个收尾单字现在允许，纯单字串联仍然禁止。`tests/host_session_tests.cpp` 中原本断言「非常快」绝不出现，现改为断言它排在首位、且「卡吨」这类无锚点字串联仍不出现；
- 逐键回归数据在 `tests/data/incremental_join_cases.tsv`。
## 12.31 v0.7.3 全拼逐字母：把打完的音节同时读成没打完

- 现象：全拼比双拼更严重。`xieza` 退回「些 血 谢 写」，`xiezaiwanchen` 退回「卸载 写在」，`jintia` 给出「浸提啊 浸提阿」，用户每经过一个合法音节候选就塌回上一个词；
- 根因：`Engine::query` 只用 `primary_parse`，而它优先取 `trailing_prefix` 为空的读法。只要输入能完整切分，`expand_input_prefix` 算出的所有前缀读法就被整体丢弃，补全路径根本不跑。全拼里这是高频场景——`chen` 是合法音节，同时也是 `cheng` 的开头；
- 修复一：拼接与补全提取成 `emit_joins_and_completions`，对完整读法调用一次；当最后一个音节还能长（`syllable_can_grow` 预计算 413 个标准音节的真前缀集合）时，把词尾按音节逐级回退再调用。回退 1 步覆盖 `chen`→`cheng`，回退 2 步覆盖已经切成 `ti'a` 的 `tian`。回退后剩余音节必须非空——单音节输入回退到空会让整行被补全淹没，`an`/`xia`/`yu` 的首选一度全丢；
- 修复二：回退读法只对全拼开启。双拼一个音节固定两键，读法不会随下一次按键生长，`小鹤` 回退成 `he` 再补全会给出键盘根本拼不出的「小黑」；
- 修复三：补全出的词库整词此前是 group 1，排在 group 0 的拼接之后，于是「我先咋」「我先砸」把真实词「我现在」压到第 8。补全整词同样覆盖全部输入，改为 group 0，由「词数少者优先」决出先后；
- 已知未解：`xie` 首选是「西鄂」而不是「些」。`xi'e` 的整词命中 group 0，`xie` 的单字是 group 2，group 比 `parse_rank` 先比较，劣质切分因此赢下首位。这是既有行为，与本次改动无关。
## 12.32 v0.7.4 卸载必须总能完成

- 现象：卸载弹出「PiInput 卸载失败：PiInput active-version directory is missing」，任何版本都卸不掉；
- 根因一：`resolve_active_version` 读 `current.json` 的 `version_id`，再要求 `versions_root/<version_id>` 存在。统一布局下 `versions_root` 就是 `bin`，程序直接放在 `bin` 下，那个子目录从来不存在，于是每次卸载都在这里抛异常。现场确认 `current.json` 写着 `0.7.3-20260819-151735-25768`，而 `bin` 下没有任何版本子目录；
- 根因二：`unregister_tsf` 找 `active/bin/piinput-profile.exe`（统一布局下等于 `bin/bin/...`）和 `developer_root/Shim/PiInputTSF.dll`（实际在 `bin/` 下），两条路径都是旧布局的，即使绕过根因一也会失败；
- 根因三：整个 `run_worker` 是「全或无」的链。反注册失败、子进程返回非零、目录删不掉、注册表项删不掉，任何一项都抛异常中止，而这些失败的原因往往正是「东西已经不在了」；
- 修复：反注册在删文件之前的顺序保留，但每一步失败只记录不中止；`locate_uninstall_tools` 在所有历史布局目录里找程序（`bin`、`bin/<版本>/bin`、`bin/Shim`、`Runtime/bin`、`Runtime/Shim`、`Dev`）；`resolve_active_version` 找不到就回退到统一布局，marker 不可读或含路径穿越时同样回退而不是抛异常——它永远不会变成一个目录名，但也永远不该挡住卸载；
- 清理补全：`current.json`（安装状态标记，保留用户数据时也要删，否则下次卸载又踩同一个坑）、`Runtime/`、`Dev/`、COM 类注册 `HKCU/Software/Classes/CLSID/{13EB305F-2DA3-4CF7-8C45-16B016B801B5}`、`HKCU/Software/PiInput`；
- 结果提示改为如实汇报：清干净就说清干净，有残留就逐条列出，不再把整个卸载叫做失败；
- 唯一保留的硬失败是 `validate_uninstall_layout`：不描述 PiInput 自己目录的布局绝不能交给递归删除。

## 12.33 v0.7.4 安装后打开设置目录

- `install()` 改为返回 `{program_root, user_data}`，安装完成对话框同时列出两者，点确定后用 `ShellExecuteW` 打开 `UserData`；
- `--silent` 不弹窗也不开目录。

## 12.34 v0.7.5 全拼超级简拼

- 需求：全拼下每个音节只打声母就能出词，三种写法——声母简拼 `zhly`、首字母简拼 `zly`、简拼全拼混合 `srf`/`sruf`/`shrfa`；
- 关键约束：词库按完整拼音串排序，靠 `lower_bound` + `starts_with` 前缀扫描。简拼不是前缀关系（`zsjs` 与 `zhi'shi'jing'shen` 只有首字母相同），现有索引一条都找不到，硬扫首字母相同的几万条又太慢，所以必须有独立索引；
- 简拼键取每个音节首字母。`zh/ch/sh` 天然归约成 `z/c/s`，只需一张索引，声母简拼查询前做同样归约即可命中，`zhly` 与 `zly` 因此给出完全相同的候选；
- 索引建在内存里，不改 `.lex` 格式：`BinaryLexicon::load` 本来就把全部词条读进内存交给 `DevLexicon`，二进制文件只是加载加速。旧词库直接可用，不需要重新编译。构建时机与既有 `ReverseWordIndex` 同一套惰性模式，第一次简拼查询时才建，约 300ms，关闭简拼的用户零成本；
- 混合输入 `simplified_pinyin_readings` 把输入切成音节序列，每个音节要么完整要么只有声母，取首字母得键，再用拼写出来的音节回过头过滤：`sruf` 的键是 `srf`，过滤条件是第 2 个音节必须是 `ru`；
- 只匹配音节数等于声母数的词。打三个声母就要三字词，不带出「输入法软件」——这与「补全不猜用户没打的音节」是同一条规则，`piinput-segment-selection` 守着它；
- 只在输入完全无法读作普通拼音时启用。`wang` 是合法音节，也能读成 w+a+n+g 四个声母，不加这条限制会让四字词把「王」埋掉；
- 简拼段自带 try/catch：`PinyinSegmenter::normalize` 对非拼音字符抛 `invalid_argument`，而调用点在 `Engine::query` 的 try 之外，异常逃逸触发 terminate，`host-session` 与 `incremental-decoder` 以 `0xc0000409` 崩溃。热路径按契约不得抛异常；
- 解析在每个位置分叉，长输入调用次数指数级。输入长度上限 32、音节数上限 8，每层循环检查已收集解析数；
- 排序：外层按消耗音节数降序，会让 `sruf` 的「收入分配」压过「输入法」、`zhly` 的「综合利用」压过「张靓颖」。用 parse rank 区分精确匹配与前缀匹配、区分不同解读，在那条比较之前定胜负；
- 设置 `[pinyin] simplified_pinyin`，默认开；
- 已知落差：排序只有词频，没有语言模型。`zsjs` 的「指示精神」排第 4、`zly` 的「张靓颖」排第 5，词都在但不如搜狗靠前；
- 设计文档 `docs/superpowers/specs/2026-08-20-piinput-simplified-pinyin-design.md`，回归数据 `tests/data/simplified_pinyin_candidates.tsv`。
## 12.35 v0.7.6 候选窗点击、设置窗口与简拼单字兜底

- **候选窗点击卡死**：候选窗由 Host 主线程创建，Windows 只向创建窗口的线程投递窗口消息。`PipeServer::run()` 同步阻塞在 `ConnectNamedPipe`，消息泵只在循环末尾、处理完一次请求后跑一次，空闲时窗口完全不处理消息——点击进队列不被派发，工具栏无反应，Windows 判定无响应。新增 `ConnectionWaiter` 把等待连接放到工作线程，主线程改用 `MsgWaitForMultipleObjects(..., QS_ALLINPUT)` 同时等连接与消息。管道保持同步：句柄一旦带 `FILE_FLAG_OVERLAPPED`，热路径上每一次读写都要改写，而那条路径已为卡顿返工多轮（v0.5.8/v0.6.0/v0.6.1）；
- 该场景本有测试 `--toolbar-responsive`，错误信息也写着 "while the pipe server was idle"，但它发完按键立刻点击，恰好赶在那一轮消息泵之前，是个竞态，因此一直是绿的。现在点击前 `Sleep(600)` 模拟真人节奏，加上等待后立刻复现失败；
- **设置窗口打不开**：`CreateMutexW` 只承诺「已存在时设 `ERROR_ALREADY_EXISTS`」，不承诺新建成功时清零 last error。调用前未 `SetLastError`，`InitCommonControlsEx` 残留的错误码被误判为「已有实例」，随即静默 `return 0`——无窗口、无提示、退出码 0。修复：调用前显式清零；且判定为「已存在」但 `FindWindow` 找不到窗口时视为残留互斥体，继续开自己的窗口。新增 `tests/settings_window_regression.ps1`；
- **简拼单字兜底**：`mk`、`dddyn` 这类纯声母输入在常规路径下完全无解析（`m` 不是合法音节，`mk` 也不是任何音节的前缀），`expand_input_prefix` 返回空，于是只有简拼候选，单字兜底那条路从未被触发——`mk` 给完 24 个词就断了。现在简拼词组之后用第一个声母的单字继续填充（复用 `query_completions_unlocked`），`mk` 48 个、`dddyn` 24 个、`sdsf` 32 个，与搜狗观感一致；
- **微信输入框遮挡**：靠 `CandidatePresenter` 已有的光标定位日志取证。坐标系要先对齐——PowerShell 默认进程是 DPI 虚拟化的，查到 2048x1152，`SetProcessDPIAware` 后才是真实的 2560x1440；据微信窗口 `x=1636~2540` 才能从日志里认出它的记录。微信报告的插入点矩形只有 2 像素高（同机普通程序报 24），`place_candidate_window` 用 `caret.bottom + gap` 锚定，于是候选窗落在文字行的下半部分。修复是锚定前把插入点补足到至少 `16 * dpi / 96`，通用规则而非微信特例——Electron 系普遍报告过小的插入点；
- 该修复与 `candidate_presenter_tests.cpp` 中 200% DPI 那条断言冲突：它用 24 像素高的 caret，在 200% 缩放下相当于半行。改为 48 像素，即该 DPI 下一行文字的真实高度，测试原本要验证的「间隙随 DPI 缩放」意图不变。
## 12.36 v0.7.7 系统托盘与全量设置窗口

- **托盘属于 Host**：它是唯一既长驻又拥有消息循环的 PiInput 进程。托盘窗口与候选窗同线程，因此完全依赖 v0.7.6 修好的消息泵——管道循环等待按键时若不泵消息，右键菜单打不开；
- 菜单项显示当前状态（「中文」「小鹤双拼」）而非动作名，菜单同时就是状态显示。左键单击切换语言；
- 切换动作**写设置文件**而不是直接改引擎状态：Host 本来就在每个输入边界重读 `settings.ini`，走同一条路可保证托盘与设置窗口行为一致。「退出」经 `PipeServer::set_stop_requested_handler` 在两次请求之间检查，不侵入管道循环；
- **设置窗口改为数据表驱动**：30 个选项写成一张 `kRows` 表（页、控件类型、绑定字段、标签、取值范围），控件创建与载入/存回各自遍历该表。新增选项只需加一行表项和两个 switch 分支。布局为五个标签页，统一的标签列与控件列；
- **写入层重写为通用键值就地更新**：`apply_assignments` 只重写自己管的键，注释、未知键、本版不认识的段原样保留，缺失的段追加到末尾。原来「每段一组 in_/found_/inserted_ 标志」的写法扩到 30 个键会散架；
- 括号样式的界面文字由「搜狗」「微信」改为直接显示 `{ }` 与 `「 」`——这两个样式与那些产品无关。配置取值名 `sogou`/`wechat` 不改，它们是数据格式，改动会让已有 `settings.ini` 失效。双拼方案名（小鹤、自然码、微软双拼、智能 ABC）是键位方案通用名，不属于品牌引用，未动；
- `windows_source_regression.cmake` 中一组检查锁的是旧窗口的实现字面量（`kRowsCombo`、`value <= 6U`、`values.visible_rows`、`BS_GROUPBOX`、分组标题）。新窗口以数据表和标签页表达同一批功能，已改为检查 `Field::visible_rows` 的 `1U, 6U` 范围、`WC_TABCONTROLW`、页名，并新增九个字段的可达性检查——比旧检查更贴近「每个选项都要能改」的真正意图；
- 重写时一度丢掉的预览圆角高亮与滚轮微调已补回；
- 未做：托盘旁独立的「中／英」指示块是 Windows 语言栏组件，需在 TSF 实现 `ITfLangBarItemButton`，与托盘是两套机制。
## 12.37 v0.7.8 候选鼠标交互与托盘中英指示

- **右键菜单点外部不消失**：候选窗是 `WS_EX_NOACTIVATE` 且 `WM_MOUSEACTIVATE` 返回 `MA_NOACTIVATE`，永远不会成为前台窗口；而 `TrackPopupMenuEx` 要求宿主窗口先在前台，否则菜单不响应自身以外的点击。新建隐藏的普通窗口 `menu_owner_` 托管菜单，弹出前 `SetForegroundWindow`，返回后补 `WM_NULL`；
- **左键点选候选**：Host 知道点中了谁，但把文字送进应用只有输入法端能做。新增 `host_select_candidate_message`，Host 回传候选 id，Shim 以 `HostKeyKind::select_candidate` 重放——与数字键同一条提交路径，不新增第二套上屏逻辑。命中判定复用右键已有的 `visible_item_rects_`；
- **托盘中/英指示**：Windows 不允许一个通知图标承载两个元素，因此它是**第二个通知图标**，与主图标共用回调窗口，字用 GDI 按当前状态实时绘制（`CreateIconIndirect`）。刻意避开 TSF 语言栏（`ITfLangBarItemButton`）——Windows 11 对传统语言栏支持已不稳定，通知图标行为可控；
- **托盘「符号」**：原实现弹说明文字属于理解偏差，用户要的是启动自己的符号程序。新增 `[general] symbol_tool`，未配置时先查程序目录下的 `yesymbol.exe`。设置窗口为此新增 `Kind::text` 行类型（EDIT 控件）。
## 12.38 v0.7.9 输入指示器改用 TSF 语言栏

- v0.7.8 把中/英做成第二个通知区域图标，实机是三个图标（溢出区两个、任务栏一个），且与产品图标拼不成一组。用户给出搜狗、微软拼音的截图为证；
- **他们用的是任务栏输入指示器，即 TSF 语言栏（`ITfLangBarItemButton`）**，按钮由 Windows 在指示器内绘制排列。`Shell_NotifyIcon` 属于任务栏另一块区域，与指示器互不相干——这是 v0.7.8 的根本错误；
- 新增 `platform/windows/tsf/lang_bar_item.cpp`：两个固定 GUID 的按钮，`TF_LBI_STYLE_SHOWNINTRAY`；语言按钮用 `GetText` 返回「中」/「英」，**文字交给 Windows 绘制**，因此自动跟随系统主题、背景透明——自绘图标做不到，这是弃用自绘的直接原因；产品按钮为 `TF_LBI_STYLE_BTN_MENU`，菜单经 `InitMenu`/`OnMenuSelect`；状态变化经 `ITfLangBarItemSink::OnUpdate` 通知重绘；
- 语言栏活在 TSF DLL（宿主应用进程）内，菜单命令由文本服务直接处理：设置与符号用 `ShellExecuteW` 启动同目录程序，方案切换写 `settings.ini`。TSF DLL 不链接引擎库，只需 `schema` 与 `symbol_tool` 两个值，因此用约二十行逐行查找读 ini，而不是把整个设置解析器拉进每个宿主进程；
- Host 端通知区域图标与 `tray_icon.*` 已删除；
- `yesymbol.exe` 经 `install(PROGRAMS ...)` 随包进入 `bin`。查找顺序：设置中的 `symbol_tool` → 程序目录。安装器不强制要求该文件存在；
- `piinput-settings-window-regression` 曾报「窗口立即退出，退出码 0」，排查为环境问题：上次测试遗留的设置窗口进程仍在，新实例正确判定「已有实例」并把已有窗口拉到前台后退出，表现与「打不开」相同。测试已加固为运行前先结束遗留进程。


## 12.39 v0.7.10 模式提示窗与中英状态一致性

- 新增 `platform/windows/tsf/mode_indicator.{h,cpp}`：切换中英时在光标处浮出 2 秒的小方块，显示**中 / a / A**。用大小写字母而非「英」，是因为大写锁定是键盘上看不出来的状态（多数笔记本无指示灯），字母本身即说明下一次按键会打出什么；
- **只有会保持的状态才提示**：Shift 切换、点图标切换、按 CapsLock。按住 Shift 打大写字母不提示——它是瞬时的，输入 `Hello` 时每个大写字母弹一次会非常吵。现有 `ShiftToggleState` 已区分「单击 Shift」与「按住 Shift 打字」，因此该性质天然成立；
- 提示窗放 TSF 端而非 Host 端：语言模式、大写锁定、光标位置（`system_caret_rect`）都在文本服务手里，不需要新增跨进程协议。窗口为 `WS_EX_LAYERED | WS_EX_TRANSPARENT | WS_EX_NOACTIVATE`，鼠标穿透且不夺取焦点；位置在**光标上方**，因为候选框在下方，中途按 CapsLock 时两者本会叠在一起；
- 渲染上两处是看图改的：不透明度 236→250（236 时底下正文透上来反而看不清）；字体质量 `CLEARTYPE_QUALITY`→`ANTIALIASED_QUALITY`（次像素渲染在分层窗口上会给字形描橙蓝彩边，该滤镜假定背景不透明）；
- **修复任务栏中/英停在旧状态**：`bind_context()` 绑定新上下文时重置 `english_mode_`，从 Host 同步模式时也直接赋值，两处都未刷新语言栏。按 Shift 切英文后再切换应用或输入法，内部已回中文而图标仍显示「英」——用户报告的「默认设了中文，每次切过来还是英文」即此。现统一走 `set_english_mode()`，值变必刷新；
- **修复默认语言未作用到前端**：`default_language` 原先只有 Host 读，TSF 一律按中文起步。新增 `default_language_is_english()`，激活与绑定上下文均按它初始化；
- **修复暗色任务栏上图标隐形**：`system_uses_dark_theme()` 在注册表读取失败时返回「亮色」，把「中」画成黑色；Windows 11 默认暗色任务栏，黑字等于看不见，表现为图标消失而非颜色不对。改为失败时按暗色处理画白字——白字在深色与中间色调上都可见，是更安全的一侧；
- `system_uses_dark_theme()` 移出匿名命名空间并在 `lang_bar_item.h` 声明供提示窗复用；`lang_bar_item.h` 中一处 `private:`/`public:`/`private:` 畸形访问修饰符序列已清理；
- 新增 `piinput-mode-indicator` 测试（8 项），CTest 61/61 通过。`piinput-host-process` 失败已查明：该测试在 `PIINPUT_TESTDATA_DIR` 存在时改用 `dicts/cache/piinput-base.lex`，该文件 2026-08-19 重新生成后已达 38 MB，冷启动 3.0–3.3 s，**正好压在 3000 ms 断言线上**（连跑四次：3272 失败 / 通过 / 3314 失败 / 3072 失败）。改动全在 TSF DLL 内，Host 未动，不在启动路径上。**门槛未调整**——词库长大就把线抬高等于把闸门关掉，该由项目决定是接受更慢的冷启动还是优化大词库加载。

## 12.40 v0.7.11 单音节输入的候选排序

- 全拼 `xie` 首选为「西鄂」（词频 6）而非「些」（词频 3752167），`xian` 则是「西安/西岸/锡安」占住前三位。此问题自 v0.7.9 起记录在案；
- 根因：`add_exact` 按 `single ? 2U : (covers_all ? 0U : 1U)` 分组，「些」是单字进第 2 组，「西鄂」两字且覆盖全输入进第 0 组；`better()` 中 `group` 先于 `parse_rank` 与 `base_weight` 比较，于是**次要切分**（`xi'e`）的生僻词压过主切分（`xie`）的常用字。「单字排在词之后」本身正确，但只在同一切分内部成立；
- **两次走错的方案，都被测试或推理拦下**：其一是把 `group` 改成只看覆盖度，`piinput-incremental-decoder` 立刻失败——它用三个同拼音 `a`、同权重的词条（安/长长/长安）断言长词在单字之前，那是同切分内的比较，必须保留；其二是在比较器内按 `same_parse` 分流（同切分比 group、跨切分比词频），结果正确但**破坏严格弱序**（A、B 同切分且 B 组更优权重更低，C 属另一切分且权重居中，则构成循环），`std::sort` 在此是未定义行为；
- 采用的方案保持比较器为**固定元组的字典序**：`add_exact` 增加 `alternative_reading` 参数，仅次要切分的调用传 `true`，其候选归入第 2 组与单字同组竞争由词频定位；同时把 `parse_rank` 从 `base_weight` 之前移到之后。第二点是必需的——只做第一点时主切分的全部单字仍因 `parse_rank` 更小而排在「西安」之前，实测把它压到 60 名开外；
- 结果：`xie` → 些，`xian` → 先/现/显/线/险，「西安」按自身词频 55003 落在第 18 位（羡 60457 与 弦 52599 之间）。30 组常用输入的前 5 候选对照，**仅这两组变化，其余 28 组逐字节相同**；
- `piinput-mode-indicator` 的「无光标回退位置」一项原先断言落点在主显示器工作区内，而实现钳制到锚点所在显示器；无光标时锚点取自前台窗口，整套连跑时那是别的测试的窗口，故偶发失败。改为用 `MonitorFromWindow` 取实际所在显示器再比对。单独连跑该测试 40 次未复现原失败，修正的是断言与实现不一致这一确定问题。

## 12.41 v0.7.12 符号与日期时间候选、生僻字、翻页收尾

- **符号可直接打名字取用**：`pai` 出 π、`qiuhe` 出 ∑、`wendu` 出 ℃℉。数据仍只有 `data/symbols.tsv` 一份，
  引擎侧新增 `set_symbol_shortcuts()`，匹配的是**解码后的读音**而不是按键，因此每种双拼方案自动支持，
  不需要第二张对照表。符号插在词典首选之后，不抢第一位；
- **日期时间做成二级列表**：候选第二位是带标记的入口（`🕗时间` / `📅日期`），选中后用纯函数重新生成
  完整格式列表替换候选，竖排一列一次显示完。复用符号中心早有的「替换候选列表」路径，候选窗未改。
  农历用 1900–2100 年月长与闰月表换算（`src/datetime_candidates.cpp`），超出范围不给农历而非外推；
- **生僻字**：构建脚本的 Rime 词表清单里从来没有大字表（`cn_dicts/41448.dict.yaml`）——Rime 把它在主词典里
  注释掉标注「按需启用」。补上后单字从 8,182 增至 40,596，权重取 1 排在所有普通词表之后。
  作为独立 `--source` 加载而非改上游主词典；为此把词库脚本回归里「一律禁止 --source」的守卫
  改成按名字禁用那几类旧词源；
- **翻页收尾**：`=` 到底停住、`-` 到顶收回单行，有候选时两键始终被吃掉，不再把字符打进正文。
  根因是 `enter_segment_selection()` 的成功判据是「候选列表非空」，而该列表**总是**含有被保留的普通候选，
  于是逐字一个新选项都没产生时也报成功，调用方重置视图后显示同一批候选，看起来像列表循环。
  判据改为 `candidates.size() > segment_candidate_offset`；失败路径同时补回 `view_mode` 还原；
- **回车**：未移动选择时仍上屏原始拼音，移动过选择后上屏选中候选；日期时间列表中始终上屏选中项；
- **代码审查修复**：`Engine` 拷贝构造漏拷符号与时钟配置（拷贝出的引擎静默失去功能）、
  语言栏诊断日志常开且每行 open/close（跑在每个宿主进程的 UI 线程）、
  提示窗窗口类在模块卸载后留下悬空 `WNDPROC`、`query()` 返回数超过调用方上限、
  `settings_value()` 每次调用都做 shell 目录查询并重解析 ini；
- **性能门槛改由宿主自测**：`--health` 新增 `startup_ms`，冷启动断言不再包含脚本自身创建的四个进程。
  实测宿主自报 1413 ms、脚本墙钟 1504 ms，差值即进程创建开销，机器繁忙时可达上千毫秒；
- **目录整理**：根目录 26 个脚本收拢到 `scripts/dev/`，只留 5 个常用入口；README 重写并加入图标。
  历史发布记录保持原路径不动——它们描述的是各自版本当时的布局。

## 12.42 v0.7.12 后续工程加固

- **敏感输入边界**：TSF 在只读编辑会话内查询 `GUID_PROP_INPUTSCOPE`。密码、私密、数字密码和 PIN 域完全透传，不发送 Host 请求、不创建候选或组合，也不进入用户学习；策略单测和 Windows 源码边界回归已加入，真实应用矩阵仍是发布前人工门槛；
- **结构化语料真正执行**：新增语料运行器，把 313 条当前相关用例逐条送入 928,712 词条引擎。84 条长输入断言有结果，46 条专业词断言目标排名，170 条当前关闭的纠错/模糊音路径做稳定性检查；另有 13 条专业词未达目标，列入 `known_missing_professional_targets.json`，不得伪装成已通过；
- **精确构建身份**：`--version` 保留 `0.7.12` 语义版本，`--build-id` 和健康响应改为 `0.7.12+<12位提交号>`，脏工作区附加 `-dirty`。安装器用完整 ID 确认新 Host，解决相同语义版本下误连旧进程；
- **诊断证据**：诊断 JSON 增加 Host 与词库 SHA-256，和原有 Shim 哈希一起形成入口、运行进程、数据三层证据；
- **测试基线**：CTest 从 61 项增加到 63 项，新增输入域策略与结构化语料回归。

## 12.43 v0.7.13 智能标点、专业词与发布工程闭环

- **13 个专业词缺口归零**：专业词补充表进入词库构建输入，结构化语料由 313 条、59 条专业词目标共同守门；旧的 `known_missing_professional_targets.json` 基线删除，不再把已补齐词条列作已知缺口；
- **大词库改为只读内存映射**：`BinaryLexicon` 直接映射 `.lex` 文件并报告 `lexicon_storage=mmap` 与映射字节数，避免把大词库整体复制进第二份堆缓冲；
- **Smart Punctuation 独立决策层**：句号、时间冒号、数字逗号、分数斜杠与中文顿号不再依赖“上一按键”布尔状态。歧义符号使用单字符 TSF provisional composition，下一数字与正文边界按序定稿；
- **Notepad++ 文档真值**：Scintilla 的 TSF 文档存储不暴露已直通落盘的数字。TSF 快照为空时改用同进程、当前焦点、只读的 Scintilla 文本窗口适配器；实机新 DLL 已验证 `1.`、`12:23`、`2/3`、`v1.0.1。`、中文冒号与反斜杠顿号；
- **Windows 发布工程**：CI、签名门禁、安装包闭包校验和稳定性采样脚本统一以 `VERSION`、精确 build ID、签名状态、包内文件白名单与安装后运行哈希为证据；
- **验证边界**：自动化通过不代替 ChatGPT、浏览器、Office、VS Code 等目标进程重新加载同一新 Shim 后的实机验收，也不代替 8 小时 soak 的最终报告。

## 12.44 v0.7.13 中文分号恢复本义

- 分号不再兼作功能命令前缀；`;;f`、`;;h`、`;;u` 与 `;关键词` 的生产路由一并删除，不保留隐藏兼容分支；
- 中文状态空闲或正在组合时，物理 `;` 都作为标点事件处理并立即提交 `；`；
- 符号中心继续通过候选工具栏、语言栏菜单、设置快捷键和双反引号加 `f` 进入，符号索引与候选能力不受影响；
- Host、会话管理、物理 TSF 控制器、随机真实输入场景和使用文档同步增加了“不再占用分号”的回归约束。

## 12.45 v0.7.13 候选 2 功能入口

- 符号入口别名：`fh`、`fuhao`、`fuh`、`fuhc`，候选 2 显示 `Ω符号`；表情入口别名：`bq`、`biaoqing`、`biaoq`、`bnqk`、`bnq`，候选 2 显示 `😜表情`；两类入口都启动随包安装或设置中指定的 `yesymbol.exe`；
- 设置入口别名：`shizhi`、`uevi`、`sz`、`shiz`、`uev`，候选 2 显示 `⚙️设置`，选择后启动同目录 `PiInput-Settings.exe`；
- 入口同时匹配解码后的完整读音和原始键码，所以全拼与小鹤双拼不维护两套候选表；某个原始别名在当前方案不可解析时，补一个普通文字候选保证动作仍在数字 2；
- 功能候选有独立 `CandidateKind` 和 `HostAction`，不当作文字提交或用户学习。Shim 把动作视为最终 cancel edit，只有 TSF 明确确认组合清除成功后才启动外部程序；清除失败恢复镜像中的原始编码，不发生焦点切换；
- 引擎逐别名、HostSession 选择、Host 协议往返和 CompositionMirror 成败路径均有自动回归；真实 TSF 应用中程序启动与焦点行为仍按 `IME-EDGE-006`～`009` 人工验收。

## 12.46 v0.7.14 中文输入使用英文标点

- 设置页原有三态标点配置改用产品语义命名：“中文标点 / 中文时使用英文标点 / 程序员标点”。第二项仍持久化为 `[punctuation] mode=english`，不新增互相冲突的布尔开关，也不改变输入语言；
- 中文输入时选择“英文标点”后，拼音组合与中文候选照常工作，Host 的完整标点表统一输出 ASCII；组合状态下会一次提交“中文候选+ASCII 标点”；
- TSF 的智能标点层此前会在 Host 之前抢先处理句号、冒号、逗号和斜杠，导致仅改 Host 设置不能保证全英文。现在 English/Programmer 标点模式明确旁路该层，避免出现冒号或逗号仍进入 provisional 的半生效状态；
- 设置解析、Host 中文候选+ASCII 标点、设置页文案和 TSF 旁路均加入自动回归；真实应用需按 `SP-MODE-001` 验证，但自动测试不得主动抢占用户桌面。

## 12.47 v0.7.14 Shift 切英文保留组合字母

- 旧逻辑收到 `switch_to_english` 后无条件清空中文会话并返回 cancel，所以中文组合中的 `cmd` 会在 Shift 切换时丢失；
- 新逻辑在清空前读取当前原始编码：非空时以一次 commit 原样提交，再把快照模式置为英文；空组合仍沿用纯 cancel 切换；
- 该行为不选择中文候选，不参与中文学习；英文候选开启和关闭两种会话均覆盖 `cmd → Shift → cmd 上屏并进入英文`。
