# PiInput v0.7.12 发布清单

## Windows x64 用户包

```text
PiInput-v0.7.12-windows-x64.zip
└── PiInput-v0.7.12-windows-x64/
    ├── PiInput-Test.exe
    ├── PiInput-Install.exe
    ├── PiInput-Uninstall.exe
    ├── 安装与使用指南.md
    ├── v0.7.12安装、使用与测试.md
    ├── 三个长文本打字测试_v0.7.12.md
    ├── 词库查询与分段取字.md
    ├── 词库更新说明.md
    ├── 稳定入口与无重启升级说明.md
    ├── query-dictionary.cmd
    ├── LICENSE_NOTICE.md
    ├── bin/
    │   ├── PiInputTSF.dll
    │   ├── PiInputHost.exe
    │   ├── PiInput-Settings.exe
    │   ├── piinput-diagnostics.exe
    │   ├── PiInput-Install.exe
    │   ├── PiInput-Uninstall.exe
    │   ├── piinput-profile.exe
    │   ├── piinput-preview.exe
    │   └── 其他开发工具
    └── data/
        ├── piinput-base.lex
        ├── english_lexicon.tsv
        ├── english_supplement.tsv
        ├── english_completion_preferences.tsv
        ├── host_protocol.json
        └── symbols.tsv
```

用户完整解压后双击最外层 `PiInput-Install.exe`。安装器自动完成：

- Windows 管理员权限确认；
- 第一次注册永久稳定的 TSF Shim 和永久语言 Profile；
- 当前用户 Windows 键盘列表注册；
- 不自动激活或切换输入法；
- 删除旧版 `PiInputHost` 登录启动项，Host 由安装器和 Shim 无窗口后台启动；
- 普通升级只切换版本化 Host；入口自身升级时仍保持固定 Shim 路径并逐字节验证；
- 排空旧 Host 请求、健康检查新 Host 并在失败时回滚；
- 自动发现唯一的旧运行目录；
- 用户设置、词库和学习数据事务迁移；
- 冲突文件保留为 `.legacy-import`；
- 失败回滚；
- 锁定旧文件安排在重启后删除。

## 发布门禁

- Windows Release 全目标构建；
- 全量 63 项 CTest；
- 独立测试台同时验证中文候选、英文候选和自由多行文本编辑，且不加载 TSF DLL；
- 407 个全拼音节和 786 条结构化语料校验，其中 313 条真实执行引擎查询；
- 全拼、小鹤双拼、增量前缀、长句和专业词汇回归；
- 全部内置符号和键盘标点回归；
- 英文候选、事务下载、学习合并和排序稳定性回归；
- 24,323 词离线英文词频库、技术补充词和用户提供英文段落逐词补全回归；
- 安装布局、迁移、回滚和路径边界回归；
- 品牌、发布元数据和 SHA-256 完整性门禁；
- 外部大词库性能与真实 SCEL 回归。
- TSF 激活不得加载完整词库，首次有效按键加载必须禁止重入；
- 新候选快照必须只显示一行，行导航后才展开。
- 稳定 Shim 不链接核心引擎、词库或候选 UI；
- Host 连续 20 次重启均恢复已确认组合串和候选；
- 诊断工具区分永久 Shim、当前 Host 与实时 Profile 状态；
- Host 健康响应必须同时给出语义版本与提交级精确构建 ID，安装器不得把同版本旧 Host 当作新 Host；
- 诊断 JSON 必须给出 Shim、Host 和词库 SHA-256；
- 密码、私密、数字密码和 PIN 输入域不得联系 Host、显示候选、创建组合或触发学习；
- 候选窗优先使用 TSF 文本插入光标坐标，只有位置不可用时才退回鼠标；
- 候选和 caret 必须属于同一会话与同一 generation，旧坐标不得移动新候选；
- 主屏左侧的负坐标显示器必须保留坐标并按对应工作区约束；
- 安装器必须逐字节验证稳定 Shim；被占用的旧文件可改名留存，但新文件和 COM 注册都必须继续使用永久固定路径，普通 Host 升级不触碰 Shim；
- 406 个用户确认的小鹤码全部可解码；
- 3500/7000 汉字在全拼和小鹤下全部可检索；
- 宿主拒绝 TSF 编辑会话时不得吞键、清空组合或覆盖选区；
- 真实长段落在全拼和小鹤下均可解析，并可逐字回退选择；
- 精确诗句优先，普通候选耗尽后进入可撤销的分段取字；
- 中文词条和标准拼音均可通过索引化词库查询工具反查；
- 候选栏尾部工具菜单只包含“符号”和“设置”，鼠标点击不抢走编辑焦点；
- 候选字体大小（10～28）与单行高度（20～72）保存后在下一次新输入时热更新，文字保持垂直居中，不重载词库；
- 全拼/小鹤双拼和默认中文/英文设置在下一次组合边界生效，当前未完成的组合不被强制改写；
- 稳定 Shim 与 Host 都不得为每条管道消息分配 1 MiB 协议上限缓冲区；
- 组合边界判断不得复制候选快照，缺失的设置文件不得在按键热路径上抛出异常；
- Host 不得重新解码自己刚编码的回包来展示候选；
- 性能基准必须按生产候选页大小（默认 90 条）测量；
- 真实词库整句逐键延迟 P95 不超过 2 毫秒、单键不超过 6 毫秒，标点后首字母同样受限；
- 三个真实长文本逐键回放（含翻行与数字选字）至少 1500 键，逐键 P95 不超过 4 毫秒、单键不超过 15 毫秒；
- 没有进行中组合的提交必须直接插入到插入点，不得为每个英文字母创建并结束一次 TSF 组合；
- 按 `=` 进入分段取字时，用户已经看到的整词行必须原样保留；
- 补全未打完的那个音节时，候选跨度不得超过“已输入完整音节数 + 1”，不猜用户还没打到的音节；
- 结尾未打完的音节必须能被补全并消费，已选中的前缀词不得因为它而永远提交不出去；
- 单个字母的补全必须覆盖以它开头的全部音节，不得被同前缀的多音节词条挤占扫描预算；
- 整串输入全部解码后，真实多字词可拼接成覆盖全部音节的候选并排在最前，最多两条；
- 拼接只能用真实多字词，单字不得作为衔接；同样覆盖全部输入时，单条真实词库条目必须排在拼接候选之前。

## 不进入用户包

- `build/`、`dist/`、`.git/`、`.vs/`；
- OBJ、PDB、LIB 等中间文件；
- 用户自行下载的 SCEL；
- 用户设置和学习数据；
- 测试源码与开发历史文档。

## 已知边界

- 当前发布 Windows x64 v0.7.12 用户测试版；
- 尚未提供正式代码签名；
- 32 位宿主和 ARM64 尚未验证；
- 当前五页图形设置界面覆盖输入、候选窗、标点符号、英文和高级选项；
- 英文候选默认关闭，所有输入功能均可离线使用；
- 数字后连续两个点只能得到 `3.。`，无法得到 `3..`。第一个点按小数点处理，第二个点回到中文句号，否则数字后就再也打不出 `。`。用户已确认保留当前取舍，只作记录。
